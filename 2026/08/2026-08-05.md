# vLLM Merged PR Report

**Report Date:** 2026-08-05 PDT

**Total Merged PRs:** 58

---

## 1. [[ROCm] Fix AITER all-reduce fusion coverage](https://github.com/vllm-project/vllm/pull/50802)


### Base Information

- **PR Number:** #50802
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-08-05 23:52:23
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50802/files) (2):**
  - `tests/compile/passes/distributed/test_fusion_all_reduce.py`
  - `vllm/compilation/passes/fusion/allreduce_rms_fusion.py`

### Summary

**What changed and why**  
This PR fixes two regressions in the ROCm AITER all-reduce fusion coverage. First, it adds a new fusion pattern for the add-RMSNorm operation when the residual output is dead, which was missing and caused incomplete fusion. Second, it restructures the grouped-FP8 test model to avoid chained quantization errors by making each fusion site independent, and updates the test to validate each site individually with appropriate tolerances. It also fixes a test infrastructure issue by using a dynamically allocated open port instead of a hard-coded port.

**Technical impact**  
The new pattern extends the fusion coverage to handle dead-residual cases, improving performance by fusing more operations. The test changes make the test more reliable and accurate, ensuring all four fusion sites are correctly validated. The port change prevents port conflicts in parallel test runs, improving test stability.

**Potential risks**  
The new pattern might not match all possible dead-residual scenarios, but it is a straightforward extension of the existing pattern. The test tolerance changes (5% envelope) are more lenient than before, which could mask minor regressions, but they are justified by the FP8 quantization noise. The port change is low risk but could affect any code that relied on the hard-coded port.

**Key insights**  
The PR demonstrates a careful approach to fixing test flakiness by isolating fusion sites and using appropriate comparison methods. The addition of the output-only pattern shows attention to production coverage, not just test fixes. The use of a dynamic port is a good practice for distributed tests.

---

## 2. [[BugFix][KV Cache] Fix hybrid prefix caching with hidden-state extraction](https://github.com/vllm-project/vllm/pull/51108)


### Base Information

- **PR Number:** #51108
- **Author:** [gcanlin](https://github.com/gcanlin)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-08-05 23:42:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51108/files) (2):**
  - `tests/v1/core/test_kv_cache_utils.py`
  - `vllm/v1/core/kv_cache_utils.py`

### Summary

**What changed and why**  
This PR fixes a bug in hybrid prefix caching where hidden-state cache layers could have block sizes that are not divisors of the common page size, causing an assertion failure in the KV cache coordinator. The fix adjusts the block size calculation for hidden-state layers to use the largest divisor of the group block size that fits within the maximum allowed block size, ensuring divisibility and correct page alignment.

**Technical impact**  
The change modifies the block size resolution logic in `get_kv_cache_groups` and adds a helper function `_largest_divisor_at_most`. It also adds a test to verify that hidden-state groups preserve hybrid prefix cache granularity. This ensures that hidden-state cache layers are correctly aligned with the common page size, preventing runtime assertion errors and enabling proper prefix caching in hybrid models.

**Potential risks**  
The new block size calculation may reduce the block size for hidden-state layers, potentially increasing memory overhead due to wasted bytes per block. The logging added provides visibility into this waste, but it could affect performance if the block size becomes too small. The change is limited to hidden-state layers and should not affect other cache types, but it may alter memory usage patterns in hybrid models.

**Key insights**  
The bug was caused by using the common page size directly to compute the hidden-state block size, which could result in a block size that is not a divisor of the group block size. The fix uses the greatest common divisor of all group block sizes as a constraint, ensuring divisibility. The added test validates the fix by checking that the resolved block sizes are correct for a hybrid configuration with hidden-state layers.

---

## 3. [[Linear] [Kernel] add block-wise scaled_mm](https://github.com/vllm-project/vllm/pull/49932)


### Base Information

- **PR Number:** #49932
- **Author:** [zufangzhu](https://github.com/zufangzhu)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 23:40:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/49932/files) (3):**
  - `tests/kernels/quantization/test_block_fp8.py`
  - `vllm/model_executor/kernels/linear/__init__.py`
  - `vllm/model_executor/kernels/linear/scaled_mm/pytorch.py`

### Summary

**What changed and why**  
Adds a new block-wise FP8 scaled_mm linear kernel (BlockWiseTorchFP8ScaledMMLinearKernel) that routes DeepSeek-style block scaling (1x128 activation, 128x128 weight) through torch._scaled_mm, providing a native PyTorch backend without extra dependencies. It registers the kernel in the FP8 block kernel map and adds a test for end-to-end correctness including M-padding.

**Technical impact**  
Introduces a new kernel path for FP8 block-scaled GEMM on CUDA SM90, with M-dimension padding to satisfy cuBLASLt requirements. The kernel is integrated into the kernel selection logic and is gated by capability checks. The test validates numerics against the native reference implementation.

**Potential risks**  
The kernel is only supported on SM90 (Hopper) for CUDA, so on other architectures it will be filtered out. The M-padding logic assumes column-major activation scales and may have edge cases for non-contiguous tensors or when M is not a multiple of 4. The use of torch._scaled_mm may have performance or correctness variations across PyTorch versions. The test uses a stub object to call the kernel method, which may not fully exercise the kernel's initialization path.

**Key insights**  
The implementation leverages PyTorch's native block-scaled GEMM support, avoiding external dependencies and per-shape tuning. The kernel is competitive in compute-bound regimes, as stated in the PR description. The test includes a non-multiple-of-4 M value to cover the padding path, which is a good practice. The code carefully handles the scale tensor layouts (column-major) and transposes B and Bs appropriately for torch._scaled_mm.

---

## 4. [[XPU] update warning of XPU Graph](https://github.com/vllm-project/vllm/pull/50236)


### Base Information

- **PR Number:** #50236
- **Author:** [zhenwei-intel](https://github.com/zhenwei-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 23:33:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50236/files) (2):**
  - `.buildkite/intel_jobs/test-intel.yaml`
  - `vllm/platforms/xpu.py`

### Summary

**What changed and why**  
This PR updates the XPU Graph warning message in vllm/platforms/xpu.py to reflect that after upgrading to oneAPI 2026.0, the previous limitations regarding memory usage and Flash Attention piecewise mode have been resolved, leaving only the single-GPU execution limitation. It also adds a new CI test step for XPU Graph in .buildkite/intel_jobs/test-intel.yaml, which runs offline inference examples with XPU Graph enabled, including a temporarily disabled multi-GPU/MXFP8 test.

**Technical impact**  
The change to the warning message is a user-facing update that clarifies current XPU Graph capabilities, reducing confusion about outdated limitations. The CI addition introduces automated testing for XPU Graph functionality, which will help catch regressions and ensure the feature works as expected on Intel GPUs. The new test step also includes source file dependencies to trigger the test when relevant files change.

**Potential risks**  
The new CI test may increase CI runtime and resource usage, and could be flaky if the XPU environment or models are not stable. The warning message change is low risk, but if the assumptions about oneAPI 2026.0 resolving the issues are incorrect, users might be misled. The disabled multi-GPU/MXFP8 test indicates that not all configurations are supported, so the CI coverage is partial.

**Key insights**  
The PR reflects a maturing XPU Graph feature, with the warning being simplified as limitations are lifted. The addition of CI tests is a positive step for maintaining feature quality. The temporary disabling of multi-GPU/MXFP8 tests suggests that those features are still in progress, and the source file dependencies ensure the test runs when relevant code changes.

---

## 5. [[Bugfix][Quantization] Fix MXFP4 conversion for FlashInfer CUTLASS](https://github.com/vllm-project/vllm/pull/51038)


### Base Information

- **PR Number:** #51038
- **Author:** [lucifer1004](https://github.com/lucifer1004)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 23:19:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51038/files) (2):**
  - `tests/kernels/moe/test_ocp_mx_moe.py`
  - `vllm/model_executor/layers/fused_moe/oracle/mxfp4.py`

### Summary

**What changed and why**  
This PR fixes a bug where the MXFP4 backend selector could choose FlashInfer CUTLASS for standard checkpoints, but the weight conversion function lacked a branch for it, causing a ValueError after model loading. The change adds a new branch in convert_weight_to_mxfp4_moe_kernel_format to handle FlashInfer CUTLASS backends, reordering fused gate/up weights from [w1; w3] to [w3; w1] and applying the appropriate scale/weight interleaving for MXFP8 or BF16 variants. It also adds regression tests for both activation variants.

**Technical impact**  
The change adds support for FlashInfer CUTLASS MXFP4 backends in the weight conversion path, enabling correct model loading and inference for these backends. It introduces a dependency on flashinfer utility functions and modifies the conversion logic to handle the different weight layout and interleaving requirements. The error message is updated to reflect the new supported backends.

**Potential risks**  
The new branch relies on flashinfer functions (block_scale_interleave, interleave_moe_weights_for_sm90_mixed_gemm, interleave_moe_scales_for_sm90_mixed_gemm) which may not be available in all environments; the tests skip if flashinfer is not installed, but runtime code may fail if flashinfer is missing. The weight reordering and interleaving logic must be correct for all input shapes and dtypes; edge cases like bias being None are handled, but there is a risk of shape mismatches if the input tensors are not as expected. The change modifies the conversion function's behavior for these backends, which could affect existing users if they were previously relying on the error to catch unsupported backends.

**Key insights**  
The fix addresses a real gap in backend support by aligning the weight layout with FlashInfer CUTLASS expectations. The use of swap_w13_to_w31 and the conditional interleaving based on backend variant shows a clear understanding of the kernel requirements. The tests provide good coverage for both MXFP8 and BF16 paths, including verification of interleaving calls. The change is well-scoped and includes necessary error message updates.

---

## 6. [[Bugfix][Spec Decode] Auto-enable async scheduling for draft models](https://github.com/vllm-project/vllm/pull/48341)


### Base Information

- **PR Number:** #48341
- **Author:** [BWAAEEEK](https://github.com/BWAAEEEK)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-08-05 22:51:40
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48341/files) (3):**
  - `tests/test_config.py`
  - `tests/v1/e2e/spec_decode/draft_model/test_draft_model.py`
  - `vllm/config/vllm.py`

### Summary

**What changed and why**  
This PR fixes a bug where draft-model speculative decoding did not enable asynchronous scheduling by default. The change adds 'draft_model' to the list of methods that are allowed to use async scheduling in VllmConfig's __post_init__, updates tests to verify this default behavior, and removes xfail markers and a custom exception that were previously used to work around the issue.

**Technical impact**  
The change alters the default configuration resolution for draft-model speculative decoding, enabling async scheduling by default. This affects runtime behavior for users using draft models, potentially improving performance. It also removes test xfails, meaning those tests now run and must pass, increasing test coverage and confidence in the feature.

**Potential risks**  
Enabling async scheduling by default could introduce new runtime issues for draft-model inference, such as increased memory usage or scheduling complexities, especially in edge cases not covered by the tests. The change is minimal (one line in config) but could have broader implications for performance and stability. The removed xfails mean that if there are latent issues, they will now surface as test failures, which is good but could cause CI failures if not fully resolved.

**Key insights**  
The fix is straightforward and addresses a configuration inconsistency. The addition of a regression test ensures the default behavior is locked in. The removal of xfails and the custom exception simplifies the test code and reflects that the underlying issue is resolved. The PR is well-tested with both single and multi-GPU scenarios, and the change is low-risk but impactful for users of draft-model speculative decoding.

---

## 7. [[XPU] Route AWQ linear through choose_mp_linear_kernel](https://github.com/vllm-project/vllm/pull/50840)


### Base Information

- **PR Number:** #50840
- **Author:** [zufangzhu](https://github.com/zufangzhu)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 22:50:11
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50840/files) (2):**
  - `.buildkite/intel_jobs/quantization.yaml`
  - `vllm/model_executor/layers/quantization/auto_awq.py`

### Summary

**What changed and why**  
This PR routes XPU AWQ linear layers through the shared choose_mp_linear_kernel path (AutoAWQMarlinLinearMethod) instead of the bespoke AutoAWQXPULinearMethod. The change aligns XPU with CPU/CUDA behavior, removes the dedicated XPU method, and adds AWQ tests to Intel XPU CI. The motivation is to prevent XPU from falling back to AutoAWQLinearMethod, which calls CUDA-only ops and causes a crash (issue #41469).

**Technical impact**  
XPU now uses the standard Marlin linear method, which converts AWQ weights to GPTQ format in process_weights_after_loading and selects the XPUwNa16LinearKernel via choose_mp_linear_kernel. This removes the custom int4_gemm_w4a16 kernel path and the dependency on vllm-xpu-kernels' AWQUtils. The change also skips Marlin verification on XPU, similar to CPU, and adds AWQ test coverage to Intel XPU CI.

**Potential risks**  
The removal of the XPU-specific method could introduce performance or correctness differences if the new kernel path is not fully equivalent on XPU. The conversion to GPTQ format may have subtle differences in weight layout or scaling. The CI change adds a new test command that could increase CI runtime or fail if the environment lacks required dependencies. There is also a risk that the new path may not handle all AWQ configurations (e.g., group size -1) as the old method did.

**Key insights**  
This PR is a refactor that unifies AWQ handling across platforms, reducing code duplication and eliminating a platform-specific bug. The change leverages existing infrastructure (choose_mp_linear_kernel) and demonstrates a pattern of reusing CPU paths for XPU when kernels are available. The addition of CI tests is important to prevent regressions. The PR also highlights the importance of avoiding CUDA-only ops in shared code paths.

---

## 8. [[XPU][Test] Support MultiConnector accuracy testing on XPU](https://github.com/vllm-project/vllm/pull/51160)


### Base Information

- **PR Number:** #51160
- **Author:** [zhenwei-intel](https://github.com/zhenwei-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 22:49:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51160/files) (2):**
  - `tests/v1/kv_connector/nixl_integration/run_multi_connector_accuracy_test.sh`
  - `tests/v1/kv_connector/nixl_integration/test_accuracy.py`

### Summary

**What changed and why**  
This PR adds support for running MultiConnector accuracy tests on XPU devices. It introduces environment variables for MAX_MODEL_LEN, KV_BUFFER_DEVICE, and NUM_CONCURRENT, and modifies the test script to use the appropriate device visibility environment variable (ZE_AFFINITY_MASK for XPU, CUDA_VISIBLE_DEVICES otherwise). It also adds the kv_buffer_device parameter to the NixlConnector configuration and sets a default max-model-len for the vLLM serve commands.

**Technical impact**  
The changes enable the test to run on XPU by allowing configuration of the KV buffer device and device visibility. The default NUM_CONCURRENT is now configurable via environment variable, and the default max-model-len is set to 8192. The fallback GPU count in get_num_gpus() is changed from 1 to 2, which may affect test behavior on systems without nvidia-smi or rocm-smi.

**Potential risks**  
The change to the default GPU count from 1 to 2 could cause tests to attempt to use two GPUs on systems that only have one, leading to failures. The new environment variables are not validated, so incorrect values could cause runtime errors. The kv_buffer_device parameter is added to the NixlConnector config, which may not be supported in all versions of the connector, potentially causing configuration errors.

**Key insights**  
The PR is a targeted enhancement to make the test infrastructure more flexible and XPU-compatible. It follows a pattern of using environment variables to override defaults, which is good for CI flexibility. However, the change to the default GPU count seems unrelated to the XPU support and could be a potential regression; it might be better to make it configurable via an environment variable as well.

---

## 9. [[XPU] Support MXFP8 linear weights for INC DeepSeek V4 model](https://github.com/vllm-project/vllm/pull/48476)


### Base Information

- **PR Number:** #48476
- **Author:** [xwu-intel](https://github.com/xwu-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 22:17:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48476/files) (1):**
  - `vllm/model_executor/kernels/linear/mxfp8/xpu.py`

### Summary

**What changed and why**  
This PR modifies the XPU MXFP8 linear kernel to support DeepSeek V4's compressed-tensors MXFP8 weights. It changes the weight scale preprocessing to store a transposed contiguous buffer that matches oneDNN's expected layout, and adds a method to precompute batched weight and scale tensors for BMM layers like wo_a, enabling efficient grouped fp8_bmm operations.

**Technical impact**  
The change alters the internal representation of weight_scale from [N, K//32] to a transposed view that yields [K//32, N] when .t() is called, avoiding an extra copy. For BMM layers, it precomputes contiguous bmm_weight [G, K, N] and bmm_scale [G, K/32, N/G] tensors, which are used by the apply_weights path. This enables the XPU path to handle DeepSeek V4's MXFP8 weights correctly and efficiently, improving performance and correctness for that model.

**Potential risks**  
The change relies on the layer having an 'is_bmm' attribute and 'bmm_batch_size' attribute; if these are not set correctly, the BMM preparation may fail or produce incorrect shapes. The transposed scale view assumes that the original weight_scale is contiguous and that .t() returns a view with the expected layout; if the underlying data is not contiguous, the view may not be efficient or correct. The change also modifies the apply_weights call to use .t() on weight and weight_scale, which could introduce overhead if the tensors are not contiguous, though the comment suggests it is zero-cost for the stored layout.

**Key insights**  
The PR cleverly uses tensor views to avoid unnecessary copies while satisfying oneDNN's layout requirements. The BMM preprocessing is a targeted optimization for grouped GEMMs, which is a common pattern in transformer models. The testing on GSM8K with a specific model shows promising accuracy, but the change is limited to the XPU kernel and may need broader validation across different models and configurations.

---

## 10. [[MM][CG] Support ViT full CUDA graph for Ernie-4.5-VL image inference](https://github.com/vllm-project/vllm/pull/45254)


### Base Information

- **PR Number:** #45254
- **Author:** [qyYue1389](https://github.com/qyYue1389)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-08-05 22:04:19
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/45254/files) (4):**
  - `docs/design/cuda_graphs_multimodal.md`
  - `examples/generate/multimodal/vision_language_offline.py`
  - `tests/models/multimodal/generation/test_vit_cudagraph.py`
  - `vllm/model_executor/models/ernie45_vl.py`

### Summary

**What changed and why**  
Adds CUDA graph support for the vision transformer (ViT) encoder in the Ernie-4.5-VL model. The change splits rotary embedding and sequence length computation into a new prepare_encoder_metadata method, implements the SupportsEncoderCudaGraph interface, and adds tests and documentation. This enables faster image inference by capturing the ViT forward pass into a CUDA graph, while keeping the resampler eager due to host-side operations.

**Technical impact**  
Introduces a new encoder CUDA graph path for Ernie-4.5-VL, with a 3.79x speedup on encoder forward time. The change refactors the ViT forward to accept precomputed metadata, adds buffer management for graph capture, and updates the model class to implement the encoder CUDA graph protocol. It also adds a test configuration and documentation entry.

**Potential risks**  
The CUDA graph path is image-only; video inputs still use eager mode, which could cause inconsistency if users expect video support. The graph replay uses padded buffers, leading to minor numerical differences (94% byte-identical outputs). The refactor changes the eager path's metadata computation, which could introduce subtle bugs if not thoroughly tested. The test uses dummy weights and a small model, so it may not catch all edge cases.

**Key insights**  
The implementation carefully separates host-side metadata computation from the graph-captured forward, using fixed buffers for rotary embeddings and sequence lengths. The resampler is intentionally kept eager because its temporal path uses numpy operations that cannot be captured. The accuracy comparison shows no significant difference, and the speedup is substantial, making this a valuable optimization for image-heavy workloads.

---

## 11. [[BugFix][Pooling] Skip weight-prefix probe when model has WeightsMapper](https://github.com/vllm-project/vllm/pull/50890)


### Base Information

- **PR Number:** #50890
- **Author:** [vhagor](https://github.com/vhagor)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 21:12:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50890/files) (2):**
  - `tests/models/test_adapters.py`
  - `vllm/model_executor/models/adapters.py`

### Summary

**What changed and why**  
This PR modifies the pooling weight loading logic in adapters.py to use the model's hf_to_vllm_mapper when probing for the correct target prefix. Previously, the probe only checked raw checkpoint names against candidate prefixes like '' and 'model.', which fails for models like Gemma4 where keys only align after applying the mapper. This caused the probe to scan the entire weight iterator, cloning every tensor, leading to excessive host memory usage (up to 44.3 GiB) and potential OOM. The change applies the mapper to each name during the probe so it can early-exit when a match is found, avoiding the full clone. A new unit test verifies the behavior.

**Technical impact**  
The change reduces memory usage during weight loading for models with a WeightsMapper by preventing unnecessary cloning of all tensors. It also improves load time (from ~24.8s to 0.7s in the A/B test). The logic is backward compatible: models without a mapper retain the original probe behavior, and the forwarded names to the parent loader remain unmapped to avoid double-applying the mapper. The new test ensures the probe uses the mapped name for membership and stops after the first hit.

**Potential risks**  
The change relies on the internal _map_name method of WeightsMapper, which may not be a stable public API. If the mapper's mapping is not a simple prefix replacement, the probe might still fail to match, but that is unlikely given the mapper's design. There is a slight risk that the probe now early-exits on a mapped name that would not have matched the original prefix, but this is intended. The test uses a custom model and may not cover all edge cases, but the logic is straightforward.

**Key insights**  
The root cause is that the generic prefix probe does not account for model-specific key remapping, leading to a full scan and clone of the checkpoint. By consulting the mapper during the probe, the code can early-exit and avoid the memory blowup. This is a targeted fix that preserves existing behavior for models without a mapper, and the new test provides regression coverage.

---

## 12. [[Bugfix][MM] Fix MiniCPM-V placeholder replacement and image processor loading on Transformers v5](https://github.com/vllm-project/vllm/pull/48413)


### Base Information

- **PR Number:** #48413
- **Author:** [YunzhuLu](https://github.com/YunzhuLu)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 21:12:01
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48413/files) (4):**
  - `tests/models/multimodal/processing/test_minicpmv.py`
  - `tests/models/registry.py`
  - `vllm/model_executor/models/minicpmv.py`
  - `vllm/multimodal/processing/processor.py`

### Summary

**What changed and why**  
This PR fixes two bugs in MiniCPM-V support for Transformers v5: (1) placeholder replacement fails because placeholders are plain text and BPE tokenization differs when encoded in context vs standalone; the fix overrides _apply_prompt_updates to fall back to segmented token encoding when token matching fails. (2) When multiple MiniCPM-V checkpoints are loaded in the same process, the wrong image processor class is reused because they share the same remote class name; the fix overrides get_hf_processor to load the processor class per repo and instantiate it directly. It also removes the max_transformers_version cap for MiniCPM-V in the test registry and adds tests for these scenarios.

**Technical impact**  
The change modifies the core multimodal processing pipeline by adding a new function apply_text_matches_as_segmented_tokens and a corresponding method in BaseMultiModalProcessor, which affects how prompt updates are applied for all models that use this path. It also introduces a cached_property _image_processor_cls in MiniCPMVProcessingInfo that dynamically loads the image processor class from the model repo, bypassing AutoImageProcessor's shared class resolution. This ensures correct processor loading per model but adds complexity and potential performance overhead due to dynamic module loading. The removal of the max_transformers_version cap enables MiniCPM-V tests to run under Transformers v5, which may expose other compatibility issues.

**Potential risks**  
The segmented token encoding fallback could produce different tokenization than the original full-prompt encoding, potentially affecting model outputs for edge cases. The dynamic class loading for image processors may fail if the repo structure changes or if trust_remote_code is not properly set. The new tests rely on network access to Hugging Face and may be flaky or slow. The change to _apply_prompt_updates is in a shared base class, so any regression could affect other multimodal models. The removal of the version cap might cause existing tests to fail under Transformers v5 if other issues exist.

**Key insights**  
The PR addresses a subtle issue with BPE tokenization of non-special-token placeholders, which is a common pitfall in multimodal models. The solution of encoding segments separately is a pragmatic workaround. The image processor reuse issue highlights a limitation of AutoImageProcessor's class-based caching when multiple checkpoints share class names but have different implementations. The PR includes targeted tests that reproduce the exact failure scenarios, which is good practice. The changes are well-documented with clear root cause analysis and test plans.

---

## 13. [[Feature] Parse request priority from HTTP header](https://github.com/vllm-project/vllm/pull/51089)


### Base Information

- **PR Number:** #51089
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 21:10:30
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51089/files) (8):**
  - `docs/serving/online_serving/openai_compatible_server.md`
  - `rust/src/server/src/routes/openai/chat_completions/convert.rs`
  - `rust/src/server/src/routes/openai/completions/convert.rs`
  - `rust/src/server/src/utils.rs`
  - `vllm/entrypoints/generate/base/serving.py`
  - `vllm/entrypoints/openai/chat_completion/serving.py`
  - `vllm/entrypoints/openai/completion/serving.py`
  - `vllm/entrypoints/openai/responses/serving.py`

### Summary

**What changed and why**  
This PR adds support for parsing request priority from the HTTP header 'X-Vllm-Priority' in the OpenAI-compatible server. The priority from the header overrides the priority specified in the JSON request body. This change is implemented across the Rust server (for chat and completions endpoints) and the Python server (for chat, completions, and responses endpoints). The motivation is to allow clients to set priority via HTTP headers, which is useful for scenarios where the body is not easily modifiable or for middleware to inject priority.

**Technical impact**  
The change introduces a new header parsing mechanism in both the Rust and Python serving stacks. In the Rust server, the priority is extracted in `resolve_request_context` and stored in `ResolvedRequestContext`, then used in the conversion functions for chat and completions. In the Python server, a static method `_get_priority` is added to the base serving class to read the header and fall back to the body priority. This affects request handling for all three OpenAI-compatible APIs (chat, completions, responses). Documentation has been updated to describe the new header.

**Potential risks**  
Potential risks include: 1) The header value is parsed as an integer; invalid values are silently ignored and fall back to the body priority, which might hide client errors. 2) The header overrides the body priority, which could be unexpected if both are provided; this behavior is documented but could cause confusion. 3) The Rust server's `resolve_request_context` now parses the header for every request, adding a small overhead. 4) There is no validation that the priority is within an acceptable range; non-zero priorities require priority scheduling, but the server may not enforce this, leading to potential misconfiguration.

**Key insights**  
The PR consistently implements the header override across both the Rust and Python serving paths, ensuring parity. The addition of unit tests for the Rust conversion functions verifies the override behavior. The documentation update clarifies the new feature. However, the lack of error handling for invalid header values (silently ignored) and the lack of validation for priority range are areas that could be improved in future iterations.

---

## 14. [[Docs] List Intel XPU attention backends](https://github.com/vllm-project/vllm/pull/51215)


### Base Information

- **PR Number:** #51215
- **Author:** [baodii](https://github.com/baodii)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 19:58:28
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51215/files) (1):**
  - `docs/getting_started/quickstart.md`

### Summary

**What changed and why**  
Added a line to the quickstart documentation listing the supported attention backends for Intel XPU platforms. This is a documentation-only change to inform users about available options.

**Technical impact**  
No runtime behavior or code changes; only documentation content is updated. The change adds clarity for users on Intel XPU hardware.

**Potential risks**  
Minimal risk; the listed backends may become outdated if new backends are added or removed, but this is a documentation maintenance concern.

**Key insights**  
The change is purely additive and does not affect functionality. It aligns with the project's practice of documenting platform-specific backend options.

---

## 15. [[Hardware] Use torch.accelerator.empty_host_cache() for host cache cl…](https://github.com/vllm-project/vllm/pull/51107)


### Base Information

- **PR Number:** #51107
- **Author:** [zhenwei-intel](https://github.com/zhenwei-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 19:27:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51107/files) (1):**
  - `vllm/distributed/parallel_state.py`

### Summary

**What changed and why**  
Replaced the CUDA-specific torch._C._host_emptyCache() call with the unified torch.accelerator.empty_host_cache() API in cleanup_dist_env_and_memory(). This change avoids warnings on XPU and other non-CUDA platforms that don't support the old API.

**Technical impact**  
The change improves cross-platform compatibility for host pinned-memory cache cleanup. It also updates the fallback warning message to reflect the new API and its required PyTorch version (>=2.9).

**Potential risks**  
If the running PyTorch version is older than 2.9, the new API may not exist, triggering the AttributeError fallback. However, the fallback handles this gracefully with a warning. There is a slight risk that torch.accelerator.empty_host_cache() behaves differently than the old CUDA-specific call, but it is expected to be equivalent.

**Key insights**  
The change aligns with PyTorch's move towards accelerator-agnostic APIs, reducing platform-specific code. The version bump in the warning message correctly reflects the introduction of the unified API.

---

## 16. [[Model Runner V2] Cache draft logits in model's LM head dtype](https://github.com/vllm-project/vllm/pull/50910)


### Base Information

- **PR Number:** #50910
- **Author:** [TheEpicDolphin](https://github.com/TheEpicDolphin)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-08-05 19:12:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50910/files) (6):**
  - `tests/v1/spec_decode/test_rejection_sampler_utils.py`
  - `tests/v1/worker/test_gpu_gumbel_sample.py`
  - `vllm/v1/worker/gpu/sample/gumbel.py`
  - `vllm/v1/worker/gpu/spec_decode/dspark/speculator.py`
  - `vllm/v1/worker/gpu/spec_decode/rejection_sampler_utils.py`
  - `vllm/v1/worker/gpu/spec_decode/speculator.py`

### Summary

**What changed and why**  
This PR changes the draft logits cache in Model Runner V2 to store pre-temperature logits in the model's LM head dtype (e.g., BF16) instead of FP32 temperature-applied logits. This halves memory usage for the cache. The temperature scaling is now applied inside the rejection sampling kernels when the cached logits are loaded. The change also renames parameters from output_processed_logits to logits_cache for clarity and updates tests to verify bitwise preservation of input logits and correct behavior across dtypes.

**Technical impact**  
Memory usage for the draft logits cache is reduced by 50% (e.g., from FP32 to BF16). The rejection sampler kernels now divide cached logits by temperature on load, which is mathematically equivalent to the previous approach but requires careful handling to avoid precision loss. The gumbel_sample kernel now stores logits before temperature application, and the rejection sampler applies temperature in multiple places (global logprobs, local stats, resampling). Tests were added to ensure bitwise equality of cached logits and that rejection sampling remains unbiased across dtypes.

**Potential risks**  
The main risk is numerical precision: storing logits in BF16/FP16 and then dividing by temperature in the kernels could introduce rounding errors that affect acceptance rates or sampling quality. The PR includes acceptance-rate verification showing no regression, but edge cases with extreme temperatures or large logits could still be problematic. Another risk is that the temperature division is now done in multiple kernel locations, and any inconsistency (e.g., different rounding modes) could cause subtle bugs. The change also touches the gumbel_sample kernel's parameter names, which could break external callers if not updated.

**Key insights**  
The PR cleverly avoids precision loss by caching pre-temperature logits, which are exactly representable in the head dtype, and applying temperature later. This is a good example of memory optimization without changing mathematical behavior. The acceptance-rate verification with counterbalanced runs is thorough and shows no regression. The test for bitwise equality is strong and ensures the cache stores the exact input logits. The change also improves code clarity by renaming parameters to reflect their new purpose.

---

## 17. [K3: remove the add operation for megamoe path](https://github.com/vllm-project/vllm/pull/51146)


### Base Information

- **PR Number:** #51146
- **Author:** [jeejeelee](https://github.com/jeejeelee)
- **Merged By:** [jeejeelee](https://github.com/jeejeelee)
- **Merged time:** 2026-08-05 18:48:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51146/files) (1):**
  - `vllm/models/kimi_k3/nvidia/model.py`

### Summary

**What changed and why**  
The PR removes the separate addition operation for the megaMoE path by folding the shared expert output into the up-projection GEMM's beta-add epilogue. This is achieved by modifying the forward method of the routed output transform to accept an optional residual tensor and use addmm_ when provided, and by computing shared expert output before the transform. The change also removes redundant comments about dimensions.

**Technical impact**  
This optimization reduces kernel launches and memory traffic by combining the shared expert addition with the up-projection GEMM, potentially improving inference performance. The change is behavior-preserving in terms of numerical output (assuming addmm_ produces the same result as separate add), and it also cleans up code comments.

**Potential risks**  
The use of addmm_ mutates the residual tensor in-place, which could introduce subtle bugs if the residual is used elsewhere or if the operation is not supported on certain hardware/dtypes. Also, the change assumes that the shared expert output shape matches the up-projection output shape; if not, it could cause runtime errors. The test results show a slight improvement in GSM8K accuracy, but this is likely due to randomness rather than the change itself.

**Key insights**  
The PR demonstrates a common optimization pattern: fusing elementwise operations into GEMM epilogues to reduce memory bandwidth and kernel overhead. The change is well-documented with a clear docstring and comments. The test results indicate no regression, and the code simplification is beneficial.

---

## 18. [[ROCm][CI] Add MLA decode accuracy and determinism tests](https://github.com/vllm-project/vllm/pull/50480)


### Base Information

- **PR Number:** #50480
- **Author:** [aarushjain29](https://github.com/aarushjain29)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-08-05 18:35:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50480/files) (2):**
  - `.buildkite/test-amd.yaml`
  - `tests/kernels/attention/test_rocm_aiter_mla_decode.py`

### Summary

**What changed and why**  
Added new ROCm-specific kernel correctness tests for the AITER MLA decode path (rocm_aiter_ops.mla_decode_fwd), which is the hot path for DeepSeek-V3/V4 inference on MI300/MI355. The tests cover smoke, accuracy vs a PyTorch reference, non-contiguous indices, and bitwise determinism. Also added CI pipeline steps for MI300 and MI355 hardware to run these tests.

**Technical impact**  
Introduces a new test file with 225 lines and modifies the CI configuration to add two new pipeline steps. The tests exercise the production kernel with varied shapes and validate numerical accuracy and determinism, filling a gap in existing coverage. CI will now run these tests on MI300 and MI355 agents, increasing test time and resource usage.

**Potential risks**  
The tests require specific ROCm hardware (MI3xx) and may be flaky if the kernel has nondeterministic behavior or if the reference implementation is not perfectly aligned. The accuracy tolerances (atol=1.5e-2, rtol=1e-2) might be too loose or too tight for BF16, potentially causing false failures or missing real regressions. The CI steps have a 180-minute timeout, which may be insufficient if the parametrized tests are slow. Also, the tests use random data without seeding in some cases (e.g., non-contiguous indices), which could lead to non-reproducible failures.

**Key insights**  
The PR adds much-needed kernel-level validation for the MLA decode path, which is critical for production inference. The reference implementation uses the absorbed MLA formulation and is straightforward. The determinism test ensures bitwise reproducibility, which is important for debugging and consistency. The CI integration is well-structured with hardware-specific steps and source file dependencies, but the test suite could benefit from more explicit seeding and possibly a smaller parametrization to reduce runtime.

---

## 19. [[MoE] Align TRTLLM MXFP4 autotune buckets](https://github.com/vllm-project/vllm/pull/50942)


### Base Information

- **PR Number:** #50942
- **Author:** [gau-nernst](https://github.com/gau-nernst)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 18:17:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50942/files) (1):**
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_mxfp4_moe.py`

### Summary

**What changed and why**  
This PR replaces the use of `moe_config.max_capture_size` (which was only 512) with a shared helper `fi_moe_largest_bucket(self.moe_config)` for setting `tune_max_num_tokens` in the TRTLLM MXFP4 MoE kernel. The change aligns the autotuning bucket size with other TRTLLM backends, allowing autotuning up to 8192 tokens, which improves prefill performance.

**Technical impact**  
The change increases the number of autotune profiles from 10 to 21, enabling better kernel selection for larger token counts. This results in a ~10% throughput improvement for TP8 prefill, though TEP8 shows a slight regression (-2.9%) possibly due to expert imbalance. The change is minimal (6 lines added, 5 removed) and uses an existing utility function, reducing code duplication.

**Potential risks**  
The TEP8 regression is a concern, though it may be due to expert imbalance rather than the change itself. The autotuning time increases (from ~1:46 to ~1:47 for more profiles), which could impact startup time. There is a risk that the new bucket sizes may not be optimal for all configurations, but the alignment with other backends reduces this risk.

**Key insights**  
The PR demonstrates the importance of consistent autotuning bucket sizes across backends for performance. Using a shared helper ensures maintainability and consistency. The performance improvement for TP8 is significant, but the TEP8 regression highlights the need for further investigation into expert distribution assumptions during autotuning.

---

## 20. [[Bugfix] Fix level-2 sleep/wake/reload with enable_lora=True](https://github.com/vllm-project/vllm/pull/39935)


### Base Information

- **PR Number:** #39935
- **Author:** [SilenNaihin](https://github.com/SilenNaihin)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-08-05 18:13:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39935/files) (11):**
  - `docs/features/sleep_mode.md`
  - `tests/basic_correctness/test_mem.py`
  - `tests/lora/test_layers.py`
  - `tests/lora/test_lora_manager.py`
  - `tests/v1/worker/test_gpu_worker_weight_transfer.py`
  - `vllm/lora/layers/base.py`
  - `vllm/lora/layers/logits_processor.py`
  - `vllm/lora/model_manager.py`
  - `vllm/v1/worker/gpu_model_runner.py`
  - `vllm/v1/worker/gpu_worker.py`
  - `vllm/v1/worker/lora_model_runner_mixin.py`

### Summary

**What changed and why**  
This PR fixes level-2 sleep/wake/reload for LoRA-enabled models. It addresses two root causes: (1) LoRA wrapping moves parameters under `base_layer`, so checkpoint names no longer match during reload; (2) LoRA GPU state (stacked tensors, adapter registry, TP>1 logits mapping) is left undefined after level-2 sleep discards memory. Changes include delegating checkpoint loading to the wrapped base layer, adding a `reset_lora_state()` method to invalidate LoRA state after weight replacement, and updating documentation to note level-2 sleep is useful when CPU memory is insufficient for level-1 backup.

**Technical impact**  
The fix modifies `BaseLayerWithLoRA.load_weights()` to delegate to the base layer's loader, preserving PyTorch module-tree semantics. It adds `reset_lora_state()` to `LoRAModelRunnerMixin`, called from both `reload_weights()` and `Worker.finish_weight_update()`, which removes all adapters and rebuilds the TP>1 logits mapping. A helper `_get_parameter_for_reload` resolves checkpoint names through LoRA wrappers for the kernel-format path. Tests cover unit-level delegation, mapping reset, and end-to-end TP=1/TP=2 sleep/reload cycles.

**Potential risks**  
The change relies on `AutoWeightsLoader` for delegation, which may not be available for all model architectures; models using flat `params_dict` loops remain unsupported. The `reset_lora_state()` drops all adapters, which could cause a performance hit if adapters are re-loaded lazily. The `_get_parameter_for_reload` helper assumes the wrapper's `base_layer` attribute exists and may not handle nested wrappers. The fix is limited to the v1 worker path; v0 may still be affected.

**Key insights**  
The PR correctly identifies that LoRA wrapping changes the module tree, and instead of overriding `named_modules()` (which would break PyTorch semantics), it uses localized delegation at load time. The `reset_lora_state()` is a comprehensive solution that handles both the reload path and the weight-transfer engine path, ensuring LoRA state is consistent after any base-weight replacement. The tests are well-designed, including a negative control to ensure the TP>1 mapping test is not vacuous.

---

## 21. [[BugFix] Dense multinode DP rescope with regression test](https://github.com/vllm-project/vllm/pull/49212)


### Base Information

- **PR Number:** #49212
- **Author:** [aoshen02](https://github.com/aoshen02)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 18:12:04
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/49212/files) (3):**
  - `tests/test_config.py`
  - `vllm/config/parallel.py`
  - `vllm/v1/engine/core.py`

### Summary

**What changed and why**  
This PR fixes a bug in dense multi-node data-parallel (DP) rescoping by introducing a helper method `reconfigure_for_independent_dp_rank` on `ParallelConfig`. Previously, the code manually set DP fields to 1/0 but did not adjust `nnodes` and `node_rank`, causing port conflicts in dense models with multi-node DP. The helper captures the correct node-level values before resetting DP fields, and it is now used in both `run_engine_core` and `EngineCoreProc.__init__`. A regression test was added to verify the reconfiguration behavior.

**Technical impact**  
The change ensures that when a non-MoE DP rank is treated as independent (DP=1), the node count and node rank are correctly set to the values within the DP group, preventing port conflicts and incorrect distributed setup. The refactor centralizes the logic, reducing duplication and improving maintainability. The new test provides coverage for the multi-node dense model scenario.

**Potential risks**  
The change modifies core engine initialization paths, which could affect existing DP configurations. If the helper's logic is incorrect for edge cases (e.g., when `nnodes_within_dp` or `node_rank_within_dp` are not properly computed), it could introduce regressions. The test could not be run locally due to missing compiled custom ops, so validation relies on CI. There is also a risk that the helper is used in contexts where the DP fields are not set as expected, but the existing code paths are limited.

**Key insights**  
The fix addresses a subtle interaction between DP rescoping and node-level configuration. By capturing `nnodes_within_dp` and `node_rank_within_dp` before modifying DP fields, the helper preserves the correct node identity for the independent DP rank. The PR also highlights the importance of regression tests for distributed configuration changes, even if they cannot be run in all environments.

---

## 22. [[Bugfix][Spec Decode] Fix NaN handling in rejection sampler tl.argmax](https://github.com/vllm-project/vllm/pull/50183)


### Base Information

- **PR Number:** #50183
- **Author:** [gabriel-peracio](https://github.com/gabriel-peracio)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 18:01:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50183/files) (2):**
  - `tests/v1/spec_decode/test_rejection_sampler_utils.py`
  - `vllm/v1/worker/gpu/spec_decode/rejection_sampler_utils.py`

### Summary

**What changed and why**  
Fixed a bug in the rejection sampler where tl.argmax on an all-NaN block could return an out-of-range index, causing illegal memory access. Added NaN-to--inf mapping and clamping in two Triton kernels, plus a regression test.

**Technical impact**  
Prevents out-of-bounds reads in _compute_global_target_argmax and _insert_resampled_kernel when target logits contain NaN. No behavior change for normal inputs; adds a small overhead of a where and minimum operation per block.

**Potential risks**  
The fix relies on tl.where and tl.minimum behavior; if Triton's NaN handling differs across versions, the fix might not fully cover edge cases. The test only covers all-NaN rows, not partial NaN or other edge cases. Performance impact is minimal but not benchmarked in isolation.

**Key insights**  
The root cause is that tl.argmax does not guarantee in-range results for all-NaN inputs, and the padded region in block decomposition makes this exploitable. The fix is surgical and defensive, and the regression test uses a non-power-of-2 vocab size to ensure padding exists.

---

## 23. [[KV Connector][Mooncake] Add tenant ID support to MooncakeStoreConnector](https://github.com/vllm-project/vllm/pull/48069)


### Base Information

- **PR Number:** #48069
- **Author:** [Lin-z-w](https://github.com/Lin-z-w)
- **Merged By:** [ivanium](https://github.com/ivanium)
- **Merged time:** 2026-08-05 17:31:18
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48069/files) (3):**
  - `docs/features/mooncake_store_connector_usage.md`
  - `tests/v1/kv_connector/unit/test_mooncake_store_worker.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/mooncake/store/worker.py`

### Summary

**What changed and why**  
This PR adds optional tenant ID support to the MooncakeStoreConnector. It reads a `tenant_id` from the Mooncake JSON config, normalizes it (trimming whitespace, defaulting to 'default' for empty/missing), and forwards it to `MooncakeDistributedStore.setup()` only when non-default. This enables multi-tenant isolation for different vLLM deployments. The change includes config parsing, setup call modification, logging updates, documentation, and tests.

**Technical impact**  
The change introduces a new `tenant_id` field to `MooncakeStoreConfig` with a default of 'default'. The `setup()` call now conditionally passes `tenant_id` as a keyword argument only when non-default, preserving backward compatibility. A new helper `_normalize_tenant_id` validates and normalizes the value, raising a `TypeError` for non-string types. Logging now includes the tenant ID. Tests cover normalization, default behavior, forwarding, and error handling.

**Potential risks**  
If a non-default tenant_id is configured but the installed Mooncake version does not support the `tenant_id` parameter, the `setup()` call will raise a `TypeError`. The PR does not catch this error to provide a clearer message, so users may see a raw TypeError. Also, the `tenant_id` is only passed to `setup()`, not to other Mooncake operations, which might be insufficient for full tenant isolation depending on Mooncake's API. The documentation mentions strict requirements but does not enforce them in code.

**Key insights**  
The design carefully maintains backward compatibility by only passing `tenant_id` when it differs from the default. The normalization logic handles edge cases like whitespace-only strings. The tests are comprehensive, covering both default and non-default scenarios, including error propagation. The documentation update clarifies multi-tenant usage and requirements.

---

## 24. [[Bugfix] Size and iterate w13 by shard count for non-gated MoE](https://github.com/vllm-project/vllm/pull/51125)


### Base Information

- **PR Number:** #51125
- **Author:** [aoshen02](https://github.com/aoshen02)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 17:24:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51125/files) (16):**
  - `tests/quantization/test_auto_gptq.py`
  - `tests/quantization/test_auto_round.py`
  - `vllm/model_executor/layers/fused_moe/config.py`
  - `vllm/model_executor/layers/quantization/auto_awq.py`
  - `vllm/model_executor/layers/quantization/auto_gptq.py`
  - `vllm/model_executor/layers/quantization/bitsandbytes.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w4a4_mxfp4.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w4a8_fp8.py`
  - `vllm/model_executor/layers/quantization/fp8.py`
  - `vllm/model_executor/layers/quantization/humming.py`
  - `vllm/model_executor/layers/quantization/inc/schemes/inc_mxfp4_moe.py`
  - `vllm/model_executor/layers/quantization/moe_wna16.py`
  - `vllm/model_executor/layers/quantization/mxfp4.py`
  - `vllm/model_executor/layers/quantization/online/fp8.py`
  - `vllm/model_executor/layers/quantization/online/moe_base.py`
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`

### Summary

**What changed and why**  
This PR fixes a bug in non-gated Mixture-of-Experts (MoE) models where the fused weight matrix w13 was incorrectly allocated and processed as if it contained two shards (gate and up projections) even when the model is non-gated (e.g., relu2_no_mul). The fix introduces a new property w13_num_shards on FusedMoEConfig that returns 2 for gated activations and 1 for non-gated, and replaces all hardcoded '2 * intermediate_size' expressions across 13 quantization files with this property. This prevents over-allocation of w13 and its associated scale/bias tensors, and fixes padding zeroing and requantization loops that incorrectly assumed two shards.

**Technical impact**  
The change is a broad refactor across 16 files, touching all major quantization methods (FP8, MXFP4, GPTQ, AWQ, bitsandbytes, etc.) and the online per-block FP8 path. For gated models, the property returns 2, so all expressions reduce to the previous behavior, ensuring bit-for-bit compatibility. For non-gated models, it corrects tensor shapes and prevents uninitialized memory from being used in quantization and GEMM operations, which could cause NaN or incorrect results. The change also adds a property to the config class, which is a clean API addition.

**Potential risks**  
The main risk is that some code paths might still assume two shards for non-gated models, such as the Marlin kernel padding in utils/marlin_utils_fp8.py, which is explicitly left out of scope. Additionally, the change touches many files, and while tests cover several gated and non-gated cases, not all hardware and quantization combinations are tested (e.g., quark on AMD, bitsandbytes, humming, inc on Gaudi). There is also a risk that some code not covered by the diff might rely on the old behavior, but the PR author has identified and excluded known related issues.

**Key insights**  
The PR correctly identifies a systemic issue where 14 quantization methods hardcoded the shard count for w13, leading to silent memory corruption in non-gated models. The solution centralizes the logic in a single property, reducing duplication and future bugs. The test plan is thorough, including synthetic and real model runs, and demonstrates that gated models are unaffected while non-gated models see correct shapes and zeroed padding. The PR also carefully scopes out related but separate issues, showing good engineering judgment.

---

## 25. [[Refactor][PCP] Make PCPManager construction extensible](https://github.com/vllm-project/vllm/pull/50066)


### Base Information

- **PR Number:** #50066
- **Author:** [pisceskkk](https://github.com/pisceskkk)
- **Merged By:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged time:** 2026-08-05 16:05:14
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50066/files) (2):**
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu/pcp_manager.py`

### Summary

**What changed and why**  
The change makes PCPManager construction extensible by adding an optional class parameter to maybe_build_pcp_manager, defaulting to PCPManager. It also adds a property pcp_manager_cls on the model runner that returns the default PCPManager class, allowing subclasses to override it. This enables out-of-tree hardware runners to use specialized PCPManager subclasses without duplicating factory logic.

**Technical impact**  
The default behavior remains unchanged, but the factory function now uses the provided class for both validation and instantiation. This introduces a new extension point for subclasses, which can enforce their own config validation. The change is minimal (9 additions, 2 deletions) and does not alter existing call sites that rely on the default.

**Potential risks**  
If a subclass overrides pcp_manager_cls but does not properly implement validate_config or the constructor signature, it could break at runtime. Also, the property is defined on the model runner, so any subclass that overrides it must ensure it returns a valid PCPManager subclass. There is a slight risk of misuse if callers pass an incompatible class, but the type hint mitigates this.

**Key insights**  
The change follows the factory method pattern to allow customization without modifying existing code. Dispatching validate_config through the same class ensures that the selected implementation's constraints are respected before instantiation. This is a clean refactor that improves extensibility for specialized hardware backends.

---

## 26. [[EPD] Remove duplicate image preprocessing in EPD and enable preprocess on GPU](https://github.com/vllm-project/vllm/pull/50390)


### Base Information

- **PR Number:** #50390
- **Author:** [gty111](https://github.com/gty111)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 15:59:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50390/files) (22):**
  - `examples/disaggregated/disaggregated_encoder/disagg_epd_proxy.py`
  - `tests/config/test_multimodal_config.py`
  - `vllm/config/ec_transfer.py`
  - `vllm/config/model.py`
  - `vllm/config/multimodal.py`
  - `vllm/config/vllm.py`
  - `vllm/distributed/ec_transfer/ec_connector/example_connector.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/model_executor/models/colqwen3.py`
  - `vllm/model_executor/models/colqwen3_5.py`
  - `vllm/model_executor/models/hunyuan_vision.py`
  - `vllm/model_executor/models/keye.py`
  - `vllm/model_executor/models/keye_vl1_5.py`
  - `vllm/model_executor/models/llava_onevision2.py`
  - `vllm/model_executor/models/minicpmv.py`
  - `vllm/model_executor/models/opencua.py`
  - `vllm/model_executor/models/qwen2_5_omni_thinker.py`
  - `vllm/model_executor/models/qwen2_vl.py`
  - `vllm/model_executor/models/qwen3_vl.py`
  - `vllm/multimodal/inputs.py`
  - `vllm/multimodal/parse.py`
  - `vllm/multimodal/processing/context.py`

### Summary

**What changed and why**  
This PR removes duplicate image preprocessing in EPD deployments by having the encoder send the patch grid metadata out-of-band via the EC connector, so the decoder no longer needs to re-run the image transform. It also adds a new --mm-processor-device flag to enable running the HF image processor on the GPU for encoder-only instances, with auto-detection based on EC role and tensor IPC transport. Supporting changes include making embedding fields optional on EC consumers, adding validation and resolution logic in config, and fixing device tensor handling in data reduction and postprocessing.

**Technical impact**  
The change introduces a new configuration flag and modifies the multimodal data parser to support optional embedding fields when embeddings come from the EC connector. It adds a request_finished override in the example connector to report grid metadata, and updates multiple model-specific parsers to declare embedding field roles. The preprocessing device resolution is integrated into VllmConfig startup, with validation to prevent GPU preprocessing on instances that also run the language model. Performance improvements are significant, with throughput gains up to 1.41x and reduced TTFT.

**Potential risks**  
The change touches many model-specific parsers and config logic, increasing the risk of regressions in multimodal input handling. The new auto device resolution depends on EC role and transport, which could lead to unexpected CPU fallback if misconfigured. The validation that rejects GPU preprocessing on non-encoder instances might break existing deployments that explicitly set device=cuda in mm_processor_kwargs. The example connector's metadata reporting relies on the processor's data being available, and falls back to full preprocessing if not, which could cause silent performance degradation. The changes to reduce_data and _postprocess_output could affect other multimodal paths.

**Key insights**  
The PR cleverly reuses the existing EC connector to pass grid metadata, avoiding duplication of work. The distinction between 'values' and 'metadata' fields in embedding declarations is a clean abstraction that allows the same parser to handle both full and metadata-only requests. The auto device resolution is well-guarded, only enabling GPU preprocessing when it is safe and beneficial. The performance measurements show that the grid out-of-band change is a uniform win, while GPU preprocessing mainly helps latency, not throughput.

---

## 27. [[ROCm] Relax MLA rope+cache test tolerances for bf16](https://github.com/vllm-project/vllm/pull/51083)


### Base Information

- **PR Number:** #51083
- **Author:** [Rohan138](https://github.com/Rohan138)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-08-05 15:50:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51083/files) (1):**
  - `tests/kernels/core/test_rotary_embedding_mla_cache_fused.py`

### Summary

**What changed and why**  
This PR relaxes test tolerances in the ROCm bf16 paths of the MLA rope+cache fused kernel test. The change is needed because the hardcoded atol=0.001 is below bf16 resolution, causing 48 failures on gfx942; the diffs are within one bf16 ULP (0.0156-0.03125), so the tolerances are adjusted to 0.02 for kv-cache, 0.04 for query, and 0.004/0.15 for fp8 cases. Only ROCm bf16 paths are affected; CUDA and other dtypes keep existing defaults.

**Technical impact**  
This is a test-only change that modifies assertions in a single test file. It does not affect kernel or model code, so there is no impact on runtime behavior, accuracy, or serving. The change improves test reliability on ROCm by aligning tolerances with the numerical precision of bf16 and fp8, reducing false failures while still catching real regressions.

**Potential risks**  
The relaxed tolerances could mask genuine numerical regressions in the bf16 path if they exceed the new bounds, but the analysis shows the diffs are within one ULP, so the risk is low. The fp8 rtol increase from 0.1 to 0.15 might hide small errors, but it is justified by e4m3 ULP. There is a slight risk that the new tolerances are too loose for other ROCm hardware or future changes, but the PR is test-only and the bounds are derived from ULP analysis.

**Key insights**  
The PR correctly identifies that the failures are due to bf16 precision limits, not a kernel bug. The use of ULP analysis to set tolerances is a sound approach. The change is well-scoped, affecting only ROCm bf16 paths, and includes a clear test plan showing before/after results. The draft status indicates ongoing review, but the reasoning is solid.

---

## 28. [[K3 Perf] Combine multiple all gather together for SP, 1.5~3x kernel level performance improvement](https://github.com/vllm-project/vllm/pull/51070)


### Base Information

- **PR Number:** #51070
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-08-05 14:42:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51070/files) (1):**
  - `vllm/models/kimi_k3/nvidia/model.py`

### Summary

**What changed and why**  
This PR optimizes sequence-parallel (SP) all-gather operations in the Kimi K3 model by combining multiple all-gathers into a single one. Previously, each auxiliary hidden state (from layers 2, 46, 90) and the final hidden state were gathered separately, incurring multiple communication rounds. Now, all sharded hidden states are concatenated along the feature dimension before a single all-gather, then split back after gathering. This reduces communication overhead and improves kernel-level performance by 1.5-3x as shown in benchmarks.

**Technical impact**  
The change reduces the number of collective communication calls from up to 4 to 1 per forward pass when SP is enabled and auxiliary states are present. This lowers latency and network traffic, improving throughput for large-scale inference. The implementation uses torch.cat and split, which are efficient and preserve correctness. The code also moves the collection of auxiliary hidden states to after SP sharding, ensuring they are sharded before packing.

**Potential risks**  
Potential risks include increased memory usage due to concatenating tensors along the feature dimension, which may be significant for large hidden sizes and token counts. The change assumes that all auxiliary states have the same hidden size as the main hidden state, which holds in this model. There is also a risk of subtle bugs in the split/concatenation logic, especially if the number of auxiliary states varies or if the order of states changes. Unit tests cover accuracy, but edge cases like zero auxiliary states or different layer configurations may not be fully tested.

**Key insights**  
The optimization leverages the fact that multiple all-gathers can be fused into one by packing data along the feature dimension, which is a common technique to reduce communication overhead in distributed training/inference. The benchmark shows significant speedups, especially for small token counts where communication dominates. The code is clean and maintains readability by using a conditional branch for the case with no auxiliary states. This change is a good example of performance optimization without altering model semantics.

---

## 29. [[MoE Refactor] Remove MoE legacy code](https://github.com/vllm-project/vllm/pull/51078)


### Base Information

- **PR Number:** #51078
- **Author:** [bnellnm](https://github.com/bnellnm)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 14:35:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51078/files) (22):**
  - `tests/v1/worker/test_gpu_model_runner_v2_eplb.py`
  - `vllm/distributed/device_communicators/base_device_communicator.py`
  - `vllm/distributed/elastic_ep/elastic_execute.py`
  - `vllm/distributed/elastic_ep/elastic_state.py`
  - `vllm/distributed/parallel_state.py`
  - `vllm/lora/layers/fused_moe.py`
  - `vllm/model_executor/layers/fused_moe/all2all_utils.py`
  - `vllm/model_executor/layers/fused_moe/fused_moe_method_base.py`
  - `vllm/model_executor/layers/fused_moe/fused_moe_modular_method.py`
  - `vllm/model_executor/layers/fused_moe/runner/moe_runner.py`
  - `vllm/model_executor/layers/fused_moe/runner/moe_runner_interface.py`
  - `vllm/model_executor/layers/fused_moe/unquantized_fused_moe_method.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w4a16_flydsl.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w4a4_nvfp4.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w8a8_fp8.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w8a8_int8.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe/compressed_tensors_moe_w8a8_mxfp8.py`
  - `vllm/model_executor/layers/quantization/fp8.py`
  - `vllm/model_executor/layers/quantization/modelopt.py`
  - `vllm/model_executor/layers/quantization/online/moe_base.py`
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
This PR removes deprecated MoE legacy code that is no longer needed after the oracle/modular kernel refactoring. It deletes the prepare_communication_buffer_for_model function and its call sites, removes the maybe_init_modular_kernel method and related abstract methods, and removes legacy quant method interfaces like maybe_make_prepare_finalize and select_gemm_impl from multiple quantized MoE implementations. The changes simplify the codebase by relying on the new modular kernel initialization flow.

**Technical impact**  
The removal of prepare_communication_buffer_for_model eliminates the explicit buffer preparation step for MoE models, which was previously required for DeepEP and other communication libraries. The new oracle-based modular kernel initialization now handles buffer preparation internally, so this change should not affect functionality if the refactoring is complete. The removal of legacy methods reduces code duplication and enforces the new interface, potentially improving maintainability and reducing the risk of using outdated code paths.

**Potential risks**  
The main risk is that some code paths might still rely on the removed functions or methods, leading to runtime errors if not fully migrated. For example, the LoRA fused_moe layer now raises a RuntimeError for old-style modular kernels, which could break if any quant method still uses that path. Additionally, the removal of prepare_communication_buffer_for_model could affect communication buffer initialization for models that still depend on it, especially in distributed settings. The test changes remove assertions related to the removed function, so test coverage for buffer preparation is reduced.

**Key insights**  
The PR is a cleanup that aligns with the ongoing MoE refactoring, moving from a manual buffer preparation step to an oracle-driven approach. The changes are mostly deletions, indicating a simplification of the codebase. The fact that many quant methods had stub implementations that raised errors suggests they were already migrated, and this PR removes the dead code. The test modifications reflect the removal of the prepare_communication_buffer_for_model call, but the core functionality of EPLB registration is still tested.

---

## 30. [[ROCm][CI] Add More AITER quantization/MoE kernel tests](https://github.com/vllm-project/vllm/pull/49375)


### Base Information

- **PR Number:** #49375
- **Author:** [micah-wil](https://github.com/micah-wil)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-08-05 14:27:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/49375/files) (5):**
  - `.buildkite/test-amd.yaml`
  - `tests/kernels/moe/test_rocm_aiter_moe.py`
  - `tests/kernels/quantization/test_rocm_fp8.py`
  - `tests/kernels/quantization/test_rocm_mxfp4.py`
  - `tests/kernels/utils.py`

### Summary

**What changed and why**  
This PR adds extensive ROCm-specific kernel tests for AITER quantization (FP8, MXFP4/FP4) and fused MoE paths, along with CI pipeline updates to run these tests on AMD hardware. It also introduces shared test utilities for accuracy and determinism checks.

**Technical impact**  
Adds over 2300 lines of test code covering ROCm-specific code paths that previously had minimal coverage. Updates CI configuration to include new test files and dependencies, ensuring these tests run in the ROCm CI pipeline. The new utilities provide reusable helpers for accuracy and determinism assertions.

**Potential risks**  
The tests are hardware-specific (gfx942/gfx950) and may fail on unsupported hardware, though they are gated with skipif. The large number of new tests could increase CI runtime. The tests rely on internal AITER APIs and environment variables, which may change and require maintenance. There is a risk of flaky tests due to tight numerical tolerances.

**Key insights**  
The PR significantly improves test coverage for ROCm-specific quantization and MoE kernels, which is crucial for ensuring correctness on AMD GPUs. The use of shared utilities promotes consistency across test files. The CI dependency updates ensure that changes to relevant source files trigger the appropriate tests.

---

## 31. [[Bugfix][Humming] Preserve ModelOpt FP8 weight dimensions](https://github.com/vllm-project/vllm/pull/51093)


### Base Information

- **PR Number:** #51093
- **Author:** [netanel-haber](https://github.com/netanel-haber)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 14:08:56
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51093/files) (2):**
  - `tests/quantization/test_modelopt.py`
  - `vllm/model_executor/layers/quantization/modelopt.py`

### Summary

**What changed and why**  
Fixed a bug in ModelOpt FP8 weight handling where after transposing serialized weights from [N, K] to [K, N], the replacement Parameter did not preserve the weight's dimension metadata (input_dim and output_dim). This caused Humming to misinterpret the tensor layout, leading to incorrect behavior. The fix explicitly sets input_dim=0 and output_dim=1 after the transpose, and adds a test to verify this behavior.

**Technical impact**  
The change ensures that the weight tensor's dimension metadata is correctly set after transposition, allowing Humming and other components to interpret the weight layout correctly. This is a behavioral fix that could affect inference correctness for ModelOpt FP8 models when using the Humming backend. The added test verifies the fix and prevents regression.

**Potential risks**  
The change is minimal and targeted, but there is a risk that other code paths relying on the previous (incorrect) behavior might be affected. However, since the fix aligns the metadata with the actual tensor layout, it is expected to be safe. The test coverage is limited to the specific scenario, so edge cases (e.g., different weight shapes or other quantization methods) are not covered.

**Key insights**  
The bug was subtle: transposing a tensor changes its logical dimensions, but the Parameter metadata (input_dim/output_dim) was not updated, causing downstream consumers to misinterpret the layout. This highlights the importance of keeping tensor metadata in sync with actual data layout. The fix is a one-liner in production code with a focused test, demonstrating a good practice of adding regression tests for bug fixes.

---

## 32. [[CI Bug] Fix `pydantic_core._pydantic_core.ValidationError: Input should be a valid integer`](https://github.com/vllm-project/vllm/pull/51179)


### Base Information

- **PR Number:** #51179
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-08-05 13:57:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51179/files) (1):**
  - `vllm/transformers_utils/model_arch_config_convertor.py`

### Summary

**What changed and why**  
The change fixes a pydantic validation error in the vLLM CI by handling the case where the 'num_experts_per_token' attribute from Hugging Face config is a list instead of an integer. The function now checks if the value is a list and returns the maximum value from the list, otherwise it returns the value as before.

**Technical impact**  
This is a bug fix that prevents a ValidationError when the model config provides a list of expert counts per token. It ensures the function returns a single integer as expected by the ModelConfig schema, allowing tests for models like HunYuanMoE to pass.

**Potential risks**  
The change assumes that when the value is a list, taking the maximum is the correct behavior. This may not be semantically correct for all models; for example, if the list represents per-layer values, the maximum might not reflect the intended global top-k. However, this is a pragmatic fix for the reported issue.

**Key insights**  
The bug was introduced by a previous PR that changed the attribute lookup to use getattr_iter, which can return a list. The fix is minimal and targeted, but it would be beneficial to add a test case covering this scenario to prevent regression.

---

## 33. [[ROCm][CI] Keep rocprofiler-sdk out of DeepEP HT MoE test workers](https://github.com/vllm-project/vllm/pull/51173)


### Base Information

- **PR Number:** #51173
- **Author:** [stefankoncarevic](https://github.com/stefankoncarevic)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-08-05 13:28:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51173/files) (1):**
  - `tests/kernels/moe/test_deepep_moe.py`

### Summary

**What changed and why**  
Added a workaround in the DeepEP MoE high-throughput test to disable rocprofiler-sdk on ROCm by setting the environment variable ROCPROFILER_REGISTER_ENABLED=0 via monkeypatch. This prevents a SIGSEGV crash at process exit caused by a ROCm HSA runtime bug when rocprofiler-sdk is attached.

**Technical impact**  
The change is limited to the test file and only affects ROCm platforms. It modifies the test to set an environment variable for the duration of the test, which is restored after the test. This should not affect production code or other tests, but it may reduce profiling capabilities during this specific test on ROCm.

**Potential risks**  
The workaround is temporary and relies on the environment variable name and behavior, which could change in future ROCm versions. It may also mask other issues if the crash is not solely due to rocprofiler-sdk. Additionally, the test now has a platform-specific branch that could become stale if the underlying ROCm bug is fixed.

**Key insights**  
The crash is not in vLLM or DeepEP but in the ROCm HSA runtime during teardown, triggered by rocprofiler-sdk. The workaround is targeted and minimal, using monkeypatch to avoid side effects. The PR correctly identifies the root cause and provides a temporary mitigation while reporting upstream.

---

## 34. [[ROCm] Work around DeepEP teardown SIGSEGV in MoE test harness](https://github.com/vllm-project/vllm/pull/51174)


### Base Information

- **PR Number:** #51174
- **Author:** [Rohan138](https://github.com/Rohan138)
- **Merged By:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged time:** 2026-08-05 12:44:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51174/files) (1):**
  - `tests/kernels/moe/parallel_utils.py`

### Summary

**What changed and why**  
Added a ROCm-specific workaround in the MoE test harness's worker function to call os._exit() after destroying the process group, bypassing a known HIP atexit handler use-after-free that causes SIGSEGV at process exit on ROCm. The change tracks an exit code to preserve pass/fail signals and flushes stdout/stderr before exiting.

**Technical impact**  
This is a test-harness-only change that alters process termination behavior on ROCm, ensuring tests pass by avoiding the crash. It introduces a platform-specific code path that affects only ROCm, with no impact on kernel/model logic or non-ROCm platforms. The exit code is preserved, so test failures are still reported correctly.

**Potential risks**  
The workaround may mask other teardown issues or hide legitimate errors that occur after destroy_process_group() but before normal exit. It relies on current_platform.is_rocm() and could break if the platform detection changes. The os._exit() call skips cleanup, potentially leaving resources unflushed, though stdout/stderr are explicitly flushed. The workaround is temporary and must be removed once the ROCm fix is available, requiring tracking.

**Key insights**  
The change is a pragmatic, targeted workaround for a known upstream bug, with clear documentation and a TODO for removal. It demonstrates a good understanding of the issue and provides a minimal, safe fix that preserves test semantics. The use of os._exit() is a common pattern to avoid problematic atexit handlers, and the explicit flushing ensures error output is not lost.

---

## 35. [[CI] Prune PyTorch Fullgraph Test](https://github.com/vllm-project/vllm/pull/51074)


### Base Information

- **PR Number:** #51074
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 12:27:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51074/files) (3):**
  - `.buildkite/test_areas/pytorch.yaml`
  - `tests/compile/fullgraph/test_basic_correctness.py`
  - `tests/compile/fullgraph/test_full_graph.py`

### Summary

**What changed and why**  
This PR prunes the PyTorch fullgraph test suite by removing redundant or problematic test cases and simplifying the test configuration. It removes the separate 'PyTorch Fullgraph' CI step that ran test_full_graph.py, updates the fullgraph smoke test to include test_full_graph.py, and modifies test_basic_correctness.py to reduce the number of test settings and streamline the compilation mode testing. It also replaces the FP8 model in test_full_graph.py with a different repository and removes quantized w8a8 models and related Blackwell skips.

**Technical impact**  
The changes reduce CI runtime and maintenance overhead by eliminating duplicate test coverage and simplifying test logic. The test_basic_correctness.py now tests all compilation modes in a single loop instead of nested loops, which may change test execution order and reduce the number of comparisons. The removal of the separate fullgraph step means test_full_graph.py is now run as part of the smoke test, potentially increasing the smoke test duration but consolidating coverage.

**Potential risks**  
Removing test cases (e.g., GPTQ quantization, BGE encoder, w8a8 models) reduces coverage for those features, potentially allowing regressions to go undetected. The change in test structure may affect test isolation and resource usage, as the smoke test now includes the heavier test_full_graph.py. The model replacement from neuralmagic to RedHatAI may have different behavior or availability, potentially causing flakiness if the model is not accessible.

**Key insights**  
The PR aims to streamline CI by removing redundant tests and consolidating fullgraph coverage. The removal of Blackwell-specific skips suggests that int8 support is no longer a concern for the tested models. The simplification of test_basic_correctness.py reduces the number of test combinations, which could speed up the suite but at the cost of less thorough validation.

---

## 36. [[MoE] Share apply_moe_activation support metadata](https://github.com/vllm-project/vllm/pull/44359)


### Base Information

- **PR Number:** #44359
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 12:05:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44359/files) (13):**
  - `tests/kernels/moe/test_cutlass_moe.py`
  - `tests/kernels/moe/test_moe.py`
  - `tests/kernels/moe/test_triton_moe_no_act_mul.py`
  - `vllm/model_executor/layers/fused_moe/__init__.py`
  - `vllm/model_executor/layers/fused_moe/activation.py`
  - `vllm/model_executor/layers/fused_moe/experts/cutlass_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/fused_humming_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/marlin_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/mxfp8_emulation_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/mxfp8_native_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/triton_moe.py`
  - `vllm/model_executor/layers/fused_moe/modular_kernel.py`
  - `vllm/model_executor/layers/fused_moe/oracle/nvfp4.py`

### Summary

**What changed and why**  
This PR centralizes MoE activation capability and configuration by introducing `apply_moe_activation_supported()` and an immutable `ApplyMoEActivationConfig` dataclass. It refactors Triton, Marlin, Humming, and CUTLASS FP8/NVFP4/MXFP4/W4A8 experts to use these shared utilities instead of duplicating activation lists and parameter plumbing. The config is resolved once on `FusedMoEExperts` from model and quantization configs, and `apply_moe_activation()` now accepts this config object instead of parallel scalar arguments. This reduces duplication and makes future activation extensions easier.

**Technical impact**  
The change modifies the public API of `apply_moe_activation()` and related functions, replacing multiple scalar parameters with a single config object. It also changes internal signatures of `run_cutlass_moe_*` and `fused_marlin_moe` functions. The `activation_func` parameter in Marlin now defaults to `None` and falls back to the shared `apply_moe_activation` with the config. The NVFP4 oracle now allows vLLM CUTLASS and Humming backends when a clamp is configured. Clamped SiLU in CUTLASS FP4/MXFP4 now uses the generic activation path instead of the fused unclamped shortcut.

**Potential risks**  
The API change could break external callers that pass the old scalar parameters to `apply_moe_activation()` or `fused_marlin_moe()`. The change in Marlin's `activation_func` default behavior might affect custom activation callbacks. The NVFP4 oracle change expands allowed backends for clamped configs, which could alter backend selection in existing deployments. The removal of the fused SiLU+quantization shortcut for clamped SiLU in FP4/MXFP4 may impact performance for those configurations.

**Key insights**  
The PR successfully reduces code duplication by centralizing activation support and configuration. The new `ApplyMoEActivationConfig` is resolved once and passed through, making the code more maintainable. The test coverage is comprehensive, including parameterized tests for all activations and backends, and the E2E Marlin regression was caught and fixed. The change is well-structured and follows the intended design of separating per-call routing tensors from static configuration.

---

## 37. [[Docker][KVConnector] Install mooncake from official wheels instead of a custom build](https://github.com/vllm-project/vllm/pull/51067)


### Base Information

- **PR Number:** #51067
- **Author:** [zhewenl](https://github.com/zhewenl)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 11:58:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51067/files) (3):**
  - `.buildkite/release-pipeline.yaml`
  - `docker/Dockerfile`
  - `docs/features/mooncake_connector_usage.md`

### Summary

**What changed and why**  
This PR removes the custom S3-hosted mooncake wheel override from the Docker build and release pipeline, instead using official PyPI wheels. It also adds a CUDA 13 variant swap in the Dockerfile, updates documentation for WITH_NVIDIA_PEERMEM and the cuda13 wheel, and cleans up related build args and env vars. The change simplifies mooncake installation and aligns Docker images with CI behavior.

**Technical impact**  
The Dockerfile now installs mooncake from official wheels, with a conditional swap to mooncake-transfer-engine-cuda13 on CUDA 13 images. The release pipeline no longer passes custom wheel URLs, reducing build complexity and avoiding duplicate installations. Documentation clarifies runtime environment variables and CUDA version compatibility.

**Potential risks**  
The official wheels may have different behavior or dependencies than the custom build, though the PR claims upstream fixes. The CUDA 13 swap relies on the mooncake version being present and may fail if the version is not available on PyPI. Removing the custom wheel could break environments that relied on its specific build flags (e.g., STORE_USE_ETCD=ON). The documentation change may confuse users if they are on CUDA 12 and follow the default install command.

**Key insights**  
The PR addresses a long-standing divergence between release images and CI by leveraging upstream fixes. The careful handling of the RUN command's exit status and the explicit WITH_NVIDIA_PEERMEM documentation show attention to operational details. The change reduces maintenance burden and improves user experience for mooncake integration.

---

## 38. [[CI Bug] Fix `Chunked prefill is required for mamba cache mode 'align'.`](https://github.com/vllm-project/vllm/pull/51177)


### Base Information

- **PR Number:** #51177
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 11:33:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51177/files) (1):**
  - `tests/quantization/test_experts_int8.py`

### Summary

**What changed and why**  
Added `enable_chunked_prefill=True` to the test configuration in `tests/quantization/test_experts_int8.py` to fix a CI failure where the Mamba cache mode 'align' requires chunked prefill. The change ensures the test passes by enabling the required prefill mode.

**Technical impact**  
This is a test-only change that modifies the test setup to satisfy a runtime assertion in VllmConfig. It does not affect production code or behavior, but it ensures the test suite runs correctly under the current configuration requirements.

**Potential risks**  
The change is minimal and low-risk. However, it may mask underlying issues if the test was intended to run without chunked prefill, but given the assertion, it is necessary. No other tests or functionality are affected.

**Key insights**  
The fix addresses a CI failure by aligning the test configuration with the required prefill mode for Mamba cache mode 'align'. This highlights the importance of keeping test configurations in sync with model-specific requirements.

---

## 39. [Revert [Misc] Avoid importing `nixl_ep` on every `vllm serve` config (#50879)](https://github.com/vllm-project/vllm/pull/51176)


### Base Information

- **PR Number:** #51176
- **Author:** [fxmarty-amd](https://github.com/fxmarty-amd)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 11:04:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51176/files) (1):**
  - `vllm/model_executor/layers/fused_moe/all2all_utils.py`

### Summary

**What changed and why**  
This PR reverts a previous change that moved imports of prepare_finalize modules (deepep, mori, nixl_ep) from inside functions to module-level conditional imports. The revert restores lazy imports inside functions to avoid importing nixl_ep on every vllm serve config, which was causing AMD CI failures due to an unrelated runtime error in aiter's fmha_v3_varlen_fwd.

**Technical impact**  
The change restores the original import behavior, reducing the risk of importing optional dependencies (like nixl_ep) at module load time. This may slightly increase import overhead when those kernels are actually used, but avoids potential side effects from importing modules that may not be needed or may cause conflicts on certain platforms.

**Potential risks**  
The revert could reintroduce the original issue that PR #50879 aimed to fix, which was likely related to import performance or circular imports. However, the PR description indicates the original change caused AMD CI failures, so reverting is intended to fix that. There is a risk that the original issue may resurface, but it is considered lower priority than the CI breakage.

**Key insights**  
This is a revert of a recent change that was merged despite failing CI. The revert is a pragmatic fix to unblock AMD CI, but it may not address the root cause of the original issue. The change is limited to one file and is straightforward, but it highlights the importance of not merging changes that break CI, even if seemingly unrelated.

---

## 40. [[CI bug] Fix `Each KV cache group's real block_size must be divisible by has h_block_size`](https://github.com/vllm-project/vllm/pull/51180)


### Base Information

- **PR Number:** #51180
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-08-05 11:03:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51180/files) (1):**
  - `vllm/v1/core/kv_cache_utils.py`

### Summary

**What changed and why**  
The PR fixes a CI failure caused by an assertion in the KV cache coordinator that requires each KV cache group's real block size to be divisible by the hash block size. The previous condition for falling back to the scheduler block size was based on whether a Mamba group's block_size differs from cache_config.block_size, but this did not catch cases where the mamba_cache_mode is not 'align', leading to invalid block sizes. The change updates the condition to check mamba_cache_mode directly, ensuring the fallback triggers correctly for non-align modes.

**Technical impact**  
This is a bug fix in the KV cache block size resolution logic. It changes the condition that determines when to use the scheduler block size instead of the computed block sizes for Mamba groups. This ensures that when Mamba groups are not in 'align' mode, the fallback is applied, preventing the assertion failure and allowing the engine to start correctly in such configurations.

**Potential risks**  
The change may alter behavior for configurations where mamba_cache_mode is not 'align' but block_size equals cache_config.block_size. Previously, such configurations would not trigger the fallback, but now they will. This could lead to using a different block size than intended, potentially affecting performance or memory usage. However, the fallback is designed to handle cases where divisibility is broken, so it should be safe.

**Key insights**  
The fix addresses a subtle interaction between Mamba cache modes and KV cache block size divisibility. The original condition was insufficient because it only checked block_size equality, not the actual mode that determines alignment. This highlights the importance of validating assumptions about cache configuration invariants, especially when multiple cache types are used together.

---

## 41. [[Bugfix] Fail fast with a clear error when CPU offload region exceeds available space](https://github.com/vllm-project/vllm/pull/50358)


### Base Information

- **PR Number:** #50358
- **Author:** [Alex-ai-future](https://github.com/Alex-ai-future)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-08-05 10:59:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50358/files) (2):**
  - `tests/v1/kv_offload/cpu/test_shared_offload_region.py`
  - `vllm/v1/kv_offload/cpu/shared_offload_region.py`

### Summary

**What changed and why**  
This PR fixes a bug where oversized CPU KV offload regions would fail with an opaque OSError from mmap.madvise. It adds a capacity check using the existing check_shm_free_space helper in the creator path, and ensures proper cleanup (unlink and close) if validation or ftruncate fails. The change also moves the joiner path to avoid redundant capacity checks and adds tests for the new error handling.

**Technical impact**  
The change modifies the SharedOffloadRegion constructor to perform a capacity check only in the creator path (after winning O_EXCL), and adds cleanup logic on failure. This prevents resource leaks and provides a clear, actionable error message when /dev/shm is insufficient. The joiner path now simply waits for the file size without re-checking capacity, reducing redundant work. Tests were added to verify the new behavior.

**Potential risks**  
The change introduces a dependency on check_shm_free_space from shm_broadcast, which may have different behavior or performance characteristics. The cleanup logic (unlink and close) could potentially interfere with concurrent joiners if not handled correctly, though the code appears to handle this by only cleaning up on creator failure. There is a small risk that the capacity check might be too conservative or not account for all memory usage scenarios, but the error message is clear.

**Key insights**  
The PR correctly identifies that the creator should be responsible for capacity validation and cleanup, while joiners should simply wait. Reusing the existing check_shm_free_space helper avoids duplicating logic. The tests are focused and cover the critical failure paths, ensuring the fix is robust.

---

## 42. [[Build] Remove Ubuntu build-stage option from the CUDA dockerfile](https://github.com/vllm-project/vllm/pull/51060)


### Base Information

- **PR Number:** #51060
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 10:50:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51060/files) (8):**
  - `.buildkite/image_build/image_build_arm64.sh`
  - `.buildkite/image_build/image_build_torch_nightly.sh`
  - `.buildkite/release-pipeline.yaml`
  - `.buildkite/scripts/hardware_ci/run-gh200-test.sh`
  - `docker/Dockerfile`
  - `docker/versions.json`
  - `docs/assets/contributing/dockerfile-stages-dependency.png`
  - `docs/getting_started/installation/gpu.cuda.inc.md`

### Summary

**What changed and why**  
This PR removes the Ubuntu build-stage option from the CUDA Dockerfile, making manylinux the only supported build path for CUDA wheels. It updates the default build base image to pytorch/manylinux2_28-builder:cuda13.0, removes the BUILD_OS argument and all Ubuntu-specific branches, and updates CI scripts, release pipeline, documentation, and versions metadata accordingly. The change aims to align the build environment with PyTorch's published wheels, ensuring a consistent glibc floor and simplifying maintenance.

**Technical impact**  
The Dockerfile now exclusively uses dnf-based package installation and the Python interpreters provided by the manylinux image, eliminating apt-based paths. This affects all CUDA wheel builds, including CI and release pipelines, which now must specify the manylinux base image. The change also updates the default BUILD_BASE_IMAGE in versions.json and adds explicit BUILD_BASE_IMAGE arguments to several CI commands that previously relied on the Ubuntu default. The final runtime images remain Ubuntu-based, preserving compatibility for deployment.

**Potential risks**  
The removal of the Ubuntu build path could break external users who relied on building with Ubuntu base images, as they will need to migrate to manylinux. The manylinux image may have different system libraries and toolchain versions, potentially causing build failures if not fully compatible with all dependencies. The CI changes are extensive and could introduce regressions if any build step is missed or if the manylinux image lacks a required package. The documentation and dependency graph updates are necessary but could be incomplete, leading to confusion.

**Key insights**  
The PR simplifies the Dockerfile by removing conditional logic, which reduces maintenance overhead and ensures a consistent build environment. The switch to manylinux aligns with PyTorch's wheel distribution, which is a strategic move to improve compatibility. The changes are well-scoped, but the removal of the Ubuntu path is a breaking change that should be clearly communicated to users. The CI updates are thorough, covering both x86_64 and aarch64, and the addition of explicit BUILD_BASE_IMAGE arguments ensures the new default is applied consistently.

---

## 43. [[KV Offload] Support out-of-tree secondary tier managers via `module_path`](https://github.com/vllm-project/vllm/pull/51007)


### Base Information

- **PR Number:** #51007
- **Author:** [ronensc](https://github.com/ronensc)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-08-05 10:30:53
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51007/files) (6):**
  - `docs/features/kv_offloading_usage.md`
  - `tests/v1/kv_offload/tiering/test_factory.py`
  - `vllm/v1/kv_offload/cpu/policies/factory.py`
  - `vllm/v1/kv_offload/factory.py`
  - `vllm/v1/kv_offload/tiering/factory.py`
  - `vllm/v1/kv_offload/tiering/spec.py`

### Summary

**What changed and why**  
Adds support for loading out-of-tree secondary tier managers via a new optional 'module_path' config key in secondary tier configurations. This mirrors existing patterns in OffloadingSpecFactory and CachePolicyFactory, allowing users to use custom tier implementations without forking vLLM or pre-registering them. Also updates logging to use warning_once/info for out-of-tree loads and adds documentation and tests.

**Technical impact**  
SecondaryTierFactory.get_tier_class now falls back to importing the module specified by 'module_path' when the tier type is not in the built-in registry. The imported class must be a subclass of SecondaryTierManager. The 'module_path' key is removed from the config before passing to the tier constructor. Logging changes reduce log noise by using warning_once and info. Documentation and tests updated accordingly.

**Potential risks**  
Potential import errors (ModuleNotFoundError, AttributeError) if module_path is incorrect or the class is not found, which are not caught and will propagate. The assert for subclassing could be bypassed if the class is not a subclass, but it's an assertion and may be disabled in optimized mode. The feature is experimental and the API may change. No validation that the module_path is a string; if non-string, importlib.import_module will raise TypeError.

**Key insights**  
The change follows an established pattern in the codebase, promoting consistency. The use of warning_once reduces log spam for repeated out-of-tree loads. The tests cover the main success and failure paths, but do not test the case where module_path is provided but type is already registered (which should use the registry). The documentation clearly explains the new feature.

---

## 44. [[Model][Frontend] Add Ling 3.0 Flash BF16, MTP, and parser support](https://github.com/vllm-project/vllm/pull/51045)


### Base Information

- **PR Number:** #51045
- **Author:** [zexplorerhj](https://github.com/zexplorerhj)
- **Merged By:** [youkaichao](https://github.com/youkaichao)
- **Merged time:** 2026-08-05 09:38:29
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51045/files) (15):**
  - `benchmarks/kernels/benchmark_moe.py`
  - `docs/models/supported_models.md`
  - `tests/models/registry.py`
  - `tests/parser/engine/test_ling3.py`
  - `vllm/config/speculative.py`
  - `vllm/model_executor/layers/fused_moe/configs/E=512,N=192,device_name=NVIDIA_H20-3e.json`
  - `vllm/model_executor/models/bailing_moe_v3.py`
  - `vllm/model_executor/models/bailing_moe_v3_mtp.py`
  - `vllm/model_executor/models/registry.py`
  - `vllm/parser/ling3.py`
  - `vllm/reasoning/__init__.py`
  - `vllm/reasoning/ling3_reasoning_parser.py`
  - `vllm/tool_parsers/__init__.py`
  - `vllm/tool_parsers/ling3_tool_parser.py`
  - `vllm/transformers_utils/model_arch_config_convertor.py`

### Summary

**What changed and why**  
This PR adds support for the Ling 3.0 Flash model (BailingMoeV3ForCausalLM) in vLLM, including BF16 inference, MTP speculative decoding, and Ling3-specific reasoning/tool-call parsing. It introduces a new model implementation (bailing_moe_v3.py) with MLA/KDA hybrid attention and MoE layers, a corresponding MTP draft model (bailing_moe_v3_mtp.py), registers the architectures in the model registry and speculative config, adds a fused MoE tuning config for NVIDIA H20, and implements Ling3 parser adapters for reasoning and tool calls.

**Technical impact**  
This is a significant feature addition that extends vLLM's model coverage to a new architecture. It integrates with existing MLA, KDA, and fused MoE kernels, and adds speculative decoding support via MTP. The changes also include new parser adapters that reuse the GLM-4.7 XML parser engine, and updates to configuration converters and registry entries. The addition of a new tuning config file for H20 indicates performance optimization for that hardware.

**Potential risks**  
The new model implementation is large (1289 lines) and may contain bugs or performance issues not caught by the limited tests. The MTP model relies on speculative config overrides that could be fragile. The parser adapters inherit from GLM-4.7, so any differences in Ling3's format could cause parsing errors. The new tuning config is hardware-specific and may not be optimal for other GPUs. There is also a risk of regressions in existing functionality due to changes in speculative config and registry.

**Key insights**  
The PR demonstrates a pattern of reusing existing components (MLA, KDA, fused MoE, GLM parser) to support a new model, which is efficient. The addition of a custom op for KDA attention outside the compiled graph is notable. The parser design defaults thinking on and handles tool calls with XML-like tags, similar to GLM-4.7. The MTP model uses a shared head and per-layer predictors, which is a common speculative decoding approach.

---

## 45. [[Kernel][Model] Optimize FA4 mm_prefix range lookup](https://github.com/vllm-project/vllm/pull/50294)


### Base Information

- **PR Number:** #50294
- **Author:** [vhagor](https://github.com/vhagor)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 07:33:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50294/files) (5):**
  - `tests/v1/attention/test_mm_prefix.py`
  - `vllm/model_executor/models/gemma4_mm.py`
  - `vllm/v1/attention/backend.py`
  - `vllm/v1/attention/backends/flash_attn.py`
  - `vllm/v1/attention/backends/utils.py`

### Summary

**What changed and why**  
This PR optimizes the FA4 attention backend's mm_prefix range lookup for Gemma4 multimodal models. It replaces the per-score loop over all padded ranges with an O(1) per-token range-id lookup, and removes max_ranges from the CuTe JIT compile key to avoid repeated recompilation when video frame counts vary. The change adds a new metadata tensor (mm_prefix_query_range_tensor) and a helper function to fill it, updates the FA4 mask_mod to use the new lookup, and adds tests.

**Technical impact**  
The change reduces the FA4 mask computation complexity from O(max_ranges) per score to O(1), and eliminates JIT recompilation caused by varying max_ranges. It introduces a persistent staging buffer for the new metadata, uses a cached mask_mod function to avoid recompilation, and adjusts the forward pass to disable causal and sliding window when using the custom mask_mod. The PR also updates Gemma4 model code to clear the new metadata field on full-attention layers.

**Potential risks**  
The new mask_mod relies on the assumption that mm_prefix ranges never overlap, which is documented but could be violated in edge cases. The change disables causal and sliding window in the FA4 forward when the mask_mod is used, which could affect correctness if not handled properly. The new metadata tensor is built on CPU and copied to GPU, adding a small overhead. The tests require FA4 and SM100, so coverage may be limited on other platforms.

**Key insights**  
The performance improvement is significant (92.8% QPS gain) and comes from both removing the range scan and avoiding JIT recompilation. The design cleverly uses per-token range bounds instead of a range-id tensor, which simplifies the kernel and avoids key-side lookups. The caching of the mask_mod function is crucial to prevent recompilation, and the use of a persistent staging buffer avoids allocations during build.

---

## 46. [[KV Offloading] Support partial-tail prefix reuse with fine-grained prefix matching](https://github.com/vllm-project/vllm/pull/50507)


### Base Information

- **PR Number:** #50507
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-08-05 07:31:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50507/files) (2):**
  - `tests/v1/kv_connector/unit/offloading_connector/test_scheduler.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading/scheduler.py`

### Summary

**What changed and why**  
This PR enhances native KV offloading to support partial-tail prefix reuse with fine-grained prefix matching. Previously, offloading could only store and restore complete physical blocks, losing reusable prefix tokens near the end of a block. The change allows preserving a fine-grained prefix boundary inside a physical block, looking it up according to prefix_match_unit, and restoring it correctly for both Attention and Mamba recurrent state. It also introduces copy-on-write for appending new tokens after restoration.

**Technical impact**  
The scheduler now supports partial-tail offloading by adding a partial_tail_boundary field to RequestOffloadState, a new _lookup method that searches for a boundary beyond complete chunks, and new boundary key generation. It also refactors TransferJob fields (renaming non_sliding_window_block_ids to deferred_fence_block_ids and sliding_window_block_ids to fenced_block_ids) and adds configuration flags (requires_cow_source, supports_partial_tail) to control when partial-tail is enabled. Tests were added to cover partial-tail store and lookup scenarios.

**Potential risks**  
The feature is gated by several conditions (blocks_per_chunk==1, uniform block sizes, no EAGLE, no self-describing events, etc.), so it should not affect configurations that don't meet these criteria. However, the new lookup logic adds complexity and could introduce off-by-one errors or incorrect boundary selection if the conditions are not fully validated. The rename of job fields could break external code that references the old names, though this is internal to the scheduler. The partial-tail path relies on copy-on-write, which may have performance implications if not handled correctly.

**Key insights**  
The PR addresses a real inefficiency in hybrid models where large physical blocks cause loss of reusable prefix tokens. By enabling partial-tail reuse, the offloading cache hit increases from 784 to 896 tokens for a 900-token prompt, improving performance. The implementation carefully restricts the feature to safe configurations and adds comprehensive unit tests to validate the new behavior.

---

## 47. [[Bugfix][Model] Fix MiniMax-M3 NVFP4 inference correctness](https://github.com/vllm-project/vllm/pull/48929)


### Base Information

- **PR Number:** #48929
- **Author:** [lucifer1004](https://github.com/lucifer1004)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 07:25:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/48929/files) (2):**
  - `tests/kernels/moe/test_flashinfer_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/flashinfer_cutlass_moe.py`

### Summary

**What changed and why**  
This PR fixes two MiniMax-M3 NVFP4 inference correctness issues. First, it adds support for the packed SWIGLUOAI_UNINTERLEAVE activation in FlashInfer by advertising the capability and forwarding alpha, beta, and clamp parameters from quant config, with a fallback to model MoE config. Second, it clarifies the physical layout contract for top-k indexer outputs: Triton uses head-major storage while SM100 MSA uses token-major, and readers normalize accordingly. Tests were added to verify parameter forwarding and layout handling.

**Technical impact**  
The change modifies the FlashInfer CUTLASS MoE expert layer to properly handle SWIGLUOAI_UNINTERLEAVE activation, including parameter plumbing and activation type mapping. It also introduces a layout contract for indexer output buffers, affecting how sparse attention indexers write and read top-k indices. This is a bug fix that improves correctness for MiniMax-M3 with NVFP4 quantization, with no changes to low-level kernels or weight formats.

**Potential risks**  
The layout contract change could affect other models or code paths that rely on the previous ambiguous buffer shape interpretation, especially when token and head dimensions are equal. The parameter fallback logic (quant-config-first, model-config-second) might alter behavior for other models using SWIGLUOAI or SWIGLUOAI_UNINTERLEAVE if they previously relied on defaults. The new tests cover the main scenarios but SM100-specific performance was not measured locally.

**Key insights**  
The PR addresses a subtle correctness issue where buffer shape alone is insufficient to determine layout when dimensions are equal, requiring an explicit contract. The parameter resolution rule is unified across FlashInfer and Marlin, ensuring consistent behavior. The addition of a test for parameter forwarding helps prevent regressions in activation-specific handling.

---

## 48. [[Bugfix] Fix MLA kv_b_proj activation dtype with Marlin FP8](https://github.com/vllm-project/vllm/pull/38771)


### Base Information

- **PR Number:** #38771
- **Author:** [jacobzhang22](https://github.com/jacobzhang22)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-08-05 07:25:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38771/files) (1):**
  - `vllm/model_executor/layers/attention/mla_attention.py`

### Summary

**What changed and why**  
This PR fixes a bug in MLA prefill where the activation dtype for kv_b_proj was incorrectly derived from the weight tensor's dtype. When Marlin repacks FP8 weights into torch.int32 on GPUs without native FP8 support, the old logic cast activations to int32, causing a runtime error. The fix introduces a helper function _get_kv_b_proj_input_dtype that returns the correct input dtype by using params_dtype when the weight is repacked, and handles other special cases (uint8, FP8 without prefill). The logic is applied in both _compute_prefill_context and _context_parallel_compute_prefill_context.

**Technical impact**  
The change ensures that kv_c_normed is cast to the appropriate compute dtype before being passed to kv_b_proj, preventing runtime failures on affected GPUs. It also centralizes the dtype selection logic, reducing duplication and making the behavior more maintainable. The helper function handles edge cases for quantized layers (AWQ/GPTQ) and NVFP4, preserving existing behavior for those cases.

**Potential risks**  
The helper function relies on the presence of params_dtype attribute, which should exist for all linear layers. There is a slight risk of behavior change for layers where weight.dtype is not int32 but is not the expected compute dtype (e.g., other repacked formats), but the logic covers known cases. The change also affects both prefill and context parallel paths, so any regression in dtype selection could impact performance or correctness, but the logic is straightforward and tested.

**Key insights**  
The root cause is that weight.dtype reflects storage format, not compute dtype, especially after Marlin repacking. Using params_dtype is the correct way to get the expected input dtype for the layer. The helper function also correctly returns None for cases where the layer quantizes internally (e.g., NVFP4 or FP8 without prefill), avoiding unnecessary casts. The added regression test ensures this specific scenario is covered.

---

## 49. [Resolve revision to commit_hash once per model load, via huggingface_hub's `resolve_revision`](https://github.com/vllm-project/vllm/pull/49990)


### Base Information

- **PR Number:** #49990
- **Author:** [Wauplin](https://github.com/Wauplin)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-08-05 06:49:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/49990/files) (9):**
  - `requirements/common.txt`
  - `requirements/test/cpu.txt`
  - `requirements/test/cuda.txt`
  - `requirements/test/rocm.txt`
  - `requirements/test/xpu.txt`
  - `vllm/config/model.py`
  - `vllm/model_executor/models/ultravox.py`
  - `vllm/transformers_utils/repo_utils.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
This PR introduces a new helper function `resolve_revision` in `vllm/transformers_utils/repo_utils.py` that uses `huggingface_hub`'s `HfApi.resolve_revision` to resolve a mutable Hugging Face revision (e.g., 'main') to a specific commit hash once during model loading. The resolved revision is then reused throughout the model initialization to avoid repeated HTTP calls and race conditions. The change also updates `vllm/config/model.py` to call this helper for both model and tokenizer revisions, and adjusts `ultravox.py` and `gpu_model_runner.py` to not carry over the revision to secondary models or when reloading weights. Additionally, it adds an explicit dependency on `huggingface_hub>=1.26.0` in `requirements/common.txt` and updates test lockfiles accordingly.

**Technical impact**  
The change reduces the number of HTTP requests to the Hugging Face Hub during model loading, which can lower rate limit issues in CI and improve load times. It also improves consistency by ensuring all components use the same commit hash, even if the branch moves during loading. The use of `ResolvedRevision` (a `str` subclass) means downstream code that expects a string revision continues to work without changes. The dependency bump to `huggingface_hub>=1.26.0` is required for the new `resolve_revision` API.

**Potential risks**  
The new `resolve_revision` function catches all exceptions and falls back to the original revision, which could mask errors if the Hub is unreachable or the repo is private. The change in `ultravox.py` sets `revision=None` for secondary models, which might change behavior if those models previously relied on the parent revision. In `gpu_model_runner.py`, setting `revision=None` when reloading weights could cause the new model to use the default revision instead of the previously specified one, which might be unexpected. The dependency bump could introduce compatibility issues if other packages are not yet compatible with `huggingface_hub` 1.26.0.

**Key insights**  
The PR leverages a new feature in `huggingface_hub` that returns a `ResolvedRevision` object, which is a string subclass that carries the resolved commit hash. This design allows the code to keep the original revision string for display purposes while using the resolved hash for downloads. The helper is designed to be best-effort and safe, falling back to the original revision on any error. The changes are well-scoped and include updates to tests and requirements, indicating a thorough review process.

---

## 50. [[Bugfix] Enable chunked prefill for qwen3.5-0.8B ppl test](https://github.com/vllm-project/vllm/pull/51153)


### Base Information

- **PR Number:** #51153
- **Author:** [music-dino](https://github.com/music-dino)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-08-05 06:21:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51153/files) (1):**
  - `tests/models/language/generation_ppl_test/test_qwen.py`

### Summary

**What changed and why**  
This PR adds a configuration flag to enable chunked prefill for the Qwen3.5-0.8B model in a specific PPL test. The change is necessary because a previous PR changed the default mamba cache mode to 'align', which requires chunked prefill, and this test was missed, causing nightly test failures.

**Technical impact**  
The change ensures the test passes by aligning the test configuration with the new default behavior. It adds a single line to set 'enable_chunked_prefill' to True for the hybrid model, which is a minimal and targeted fix.

**Potential risks**  
The risk is low, as the change is limited to a test configuration and does not affect production code. However, if the model's behavior changes in the future, this hardcoded flag might become unnecessary or need adjustment.

**Key insights**  
The PR highlights the importance of updating all affected tests when changing default behaviors. It also shows that hybrid models with mamba cache mode 'align' require chunked prefill, which is a configuration detail that can easily be overlooked.

---

## 51. [[BugFix][K3] Skip moe_intermediate padding when EP is enabled](https://github.com/vllm-project/vllm/pull/51131)


### Base Information

- **PR Number:** #51131
- **Author:** [ZeldaHuang](https://github.com/ZeldaHuang)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-08-05 02:22:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51131/files) (1):**
  - `vllm/models/kimi_k3/nvidia/model.py`

### Summary

**What changed and why**  
The change modifies the condition for padding the MoE intermediate size in the Kimi K3 model. Previously, padding was applied whenever tensor parallelism (TP) size was greater than 1, but now it is skipped when expert parallelism (EP) is enabled. This fixes an issue where padding was incorrectly applied in EP scenarios, likely causing memory or computation inefficiencies.

**Technical impact**  
This is a bug fix that alters the model initialization behavior for configurations with both TP and EP. It ensures that the MoE intermediate size is not padded when EP is enabled, which can reduce memory usage and improve performance. The change is minimal (one line) and only affects the condition for padding.

**Potential risks**  
The risk is low, but there is a possibility that in some EP configurations, padding might still be needed for correctness or performance. The change assumes that EP handles the partitioning correctly without padding, which should be verified with tests. If EP is enabled without TP (tp_size=1), the condition still evaluates to false, so padding is skipped as before.

**Key insights**  
The fix addresses a specific issue in the Kimi K3 model where EP and TP interaction caused unnecessary padding. The change is straightforward and aligns with the intent of avoiding padding when expert parallelism is used. The PR lacks detailed test results, but the change is small and targeted.

---

## 52. [[CI] Fix CI authorization notification fallback](https://github.com/vllm-project/vllm/pull/51095)


### Base Information

- **PR Number:** #51095
- **Author:** [khluu](https://github.com/khluu)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 01:34:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51095/files) (5):**
  - `.github/workflows/add_label_automerge.yml`
  - `.github/workflows/notify-ci-authorized.yml`
  - `.github/workflows/record-ci-approval.yml`
  - `.github/workflows/scripts/run_ci_command.py`
  - `.github/workflows/scripts/test_run_ci_command.py`

### Summary

**What changed and why**  
This PR fixes a race condition in CI authorization notifications where approval notifications were skipped when workflow_run.pull_requests was empty or unrelated. It adds a fallback to resolve the PR from the head commit, allows the 'ready' label to trigger a notification if the approval notification was missed, adds a unique concurrency fallback for approval-triggered runs, and moves several workflows to the self-hosted vllm-runners pool.

**Technical impact**  
The change modifies the notify-ci-authorized workflow's concurrency group and job condition, updates the run_ci_command.py script to resolve PRs from head commit, and adjusts the add_label_automerge and record-ci-approval workflows to use self-hosted runners. Tests were added and updated to cover the new behavior.

**Potential risks**  
Potential risks include: the new resolve_workflow_run_pr function may make additional API calls, potentially hitting rate limits; the concurrency fallback using workflow_run.id could cause duplicate notifications if the same PR triggers multiple workflow runs; moving to self-hosted runners may introduce infrastructure dependencies; the removal of the has_trusted_approval check in the label path could lead to duplicate notifications in some edge cases.

**Key insights**  
The root cause was a race between the approval recorder and the notification job, where the notification was skipped due to empty pull_requests, and the later 'ready' label suppressed the comment because a trusted approval existed. The fix ensures notifications are sent even when PR association is missing, while preserving deduplication via markers.

---

## 53. [[Rust Frontend] Deduplicate request preprocessing for `/tokenize`](https://github.com/vllm-project/vllm/pull/50448)


### Base Information

- **PR Number:** #50448
- **Author:** [sagearc](https://github.com/sagearc)
- **Merged By:** [BugenZhao](https://github.com/BugenZhao)
- **Merged time:** 2026-08-05 01:27:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50448/files) (5):**
  - `rust/src/chat/src/lib.rs`
  - `rust/src/server/src/routes/tests.rs`
  - `rust/src/server/src/routes/tokenize.rs`
  - `rust/src/server/src/routes/tokenize/types.rs`
  - `rust/src/text/src/lib.rs`

### Summary

**What changed and why**  
This PR refactors the /tokenize endpoint to reuse the same request preprocessing pipeline as inference, eliminating duplicate prompt handling. It exposes request processors from the chat and text crates, adds a tokenize method to the text processor, and updates the tokenize routes to use these new methods. This ensures token IDs from /tokenize match what generation would receive, especially for chat rendering and multimodal prompts.

**Technical impact**  
The change centralizes prompt tokenization logic, reducing code duplication and potential drift between tokenize and generation. It introduces new public methods (request_processor, tokenize, prepare_for_tokenization) and modifies the tokenize routes to use them. The completion tokenize path now constructs a TextRequest and uses the processor's tokenize method, while the chat path uses prepare_for_tokenization. This may affect performance slightly due to additional validation and request construction, but likely negligible.

**Potential risks**  
Potential risks include: the new tokenize method in TextRequestProcessor validates the request, which may reject requests that were previously accepted (e.g., missing required fields). The completion path now uses a TextRequest with default sampling_params and decode_options, which might alter behavior if those defaults differ from previous direct tokenizer usage. The chat path now goes through prepare_for_tokenization, which includes validation and rendering; any errors in that pipeline will now surface in /tokenize, potentially causing new failures. The tests added cover tools and max_model_len edge cases, but other edge cases may be untested.

**Key insights**  
The PR successfully deduplicates preprocessing logic, aligning /tokenize with generation. The new tests ensure that /tokenize works with tools (which previously required an output parser) and allows prompts at or above max_model_len, which is important for token counting. The refactor also exposes request processors, enabling future reuse. The change is well-structured but introduces a new public API surface that should be documented.

---

## 54. [[KV Connector][Mooncake] Add store group semantics](https://github.com/vllm-project/vllm/pull/44956)


### Base Information

- **PR Number:** #44956
- **Author:** [bitborne](https://github.com/bitborne)
- **Merged By:** [ivanium](https://github.com/ivanium)
- **Merged time:** 2026-08-05 01:25:39
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/44956/files) (2):**
  - `tests/v1/kv_connector/unit/test_mooncake_store_worker.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/mooncake/store/worker.py`

### Summary

**What changed and why**  
This PR adds opt-in group semantics to the Mooncake KV connector in vLLM. It introduces a new configuration flag 'enable_group_semantics' that, when enabled and supported by the installed Mooncake package, sets a shared logical group ID on each object passed to batch_put_from_multi_buffers. The group ID is derived from the logical KV cache identity (model, cache prefix, and chunk hash) while excluding physical sharding ranks (tp_rank, pp_rank) so that related physical objects can be grouped together. This enables Mooncake to apply group-aware metadata routing, lease refresh, and eviction behavior without changing the default write path.

**Technical impact**  
The change modifies the MooncakeStoreWorker and KVCacheStoreSendingThread to conditionally generate and pass group_ids. It adds runtime capability detection for ReplicateConfig.group_ids, a helper to construct group IDs, and updates both the main store path and the partial tail offload path to maintain alignment between keys and group_ids after filtering. The default behavior remains unchanged when the flag is disabled or unsupported, preserving backward compatibility. Tests were added to cover the new functionality, including fallback and alignment scenarios.

**Potential risks**  
Potential risks include: (1) If the Mooncake package version is misdetected, group_ids might be set on an object that doesn't support it, causing runtime errors; the detection uses hasattr which could be fooled by properties or dynamic attributes. (2) The group ID format is a new string convention; if Mooncake expects a specific format or length, it could cause issues. (3) The code mutates self.replicate_config.group_ids in place, which could affect concurrent operations if the config is shared. (4) The fallback path when group_ids is unsupported silently disables the feature, which might mask configuration errors. (5) The change touches the hot path of KV storage, and the extra list comprehensions and string operations could introduce minor performance overhead.

**Key insights**  
The implementation carefully preserves the existing write path when the feature is disabled or unsupported, ensuring backward compatibility. The group ID design correctly separates logical identity from physical sharding, which is essential for grouping related objects. The tests are comprehensive, covering detection, generation, filtering alignment, and fallback scenarios. The use of a capability detection function that checks both class and instance attributes is a robust approach to handle different Mooncake package versions.

---

## 55. [[ROCm][Bugfix] Kimi-K3 Fix KDA NaN on mixed batches and racy autotune config](https://github.com/vllm-project/vllm/pull/50649)


### Base Information

- **PR Number:** #50649
- **Author:** [kliuae](https://github.com/kliuae)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-08-05 01:07:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50649/files) (3):**
  - `vllm/models/kimi_k3/amd/kda.py`
  - `vllm/models/kimi_k3/amd/linear.py`
  - `vllm/models/kimi_k3/amd/ops/third_party/kda/chunk.py`

### Summary

**What changed and why**  
This PR fixes two correctness bugs in the ROCm Kimi-K3 KDA path. First, it separates decode sequences from mixed decode+prefill batches and routes them through fused_recurrent_kda instead of the chunk kernel, which was producing NaNs for decode sequences. Second, it removes num_stages=4 from the autotuning configuration of recompute_w_u_fwd_kernel to avoid a race condition that caused large garbage values. It also introduces a new ROCm-specific KimiK3DeltaAttention class and updates the linear layer to use it when use_full_rank_gate is set.

**Technical impact**  
The changes introduce a new file (kda.py) with a custom attention implementation for ROCm, modify the linear layer to conditionally use this new class, and adjust autotuning parameters. This improves correctness and accuracy (gsm8k score improved from 0.89 to 0.97) but adds code duplication and platform-specific logic. The autotune change may affect performance by reducing the search space.

**Potential risks**  
The new KimiK3DeltaAttention class is ROCm-specific and may not be tested on other platforms. The autotune change could impact performance on other GPUs or for other batch sizes. The separation of decode sequences might introduce overhead or edge cases if not handled correctly. The new code is large (530 lines) and may have untested paths.

**Key insights**  
The PR addresses subtle GPU-specific bugs (race conditions and NaN issues) that are hard to detect. The improvement in accuracy is significant, indicating the bugs were causing real degradation. The approach of creating a separate ROCm-specific implementation highlights the challenges of maintaining cross-platform kernel code. The autotune fix is a pragmatic workaround for a hardware-specific race condition.

---

## 56. [[Perf][KV Offload] Avoid quadratic ARC batch eviction](https://github.com/vllm-project/vllm/pull/50992)


### Base Information

- **PR Number:** #50992
- **Author:** [mindungil](https://github.com/mindungil)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-08-05 00:58:37
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50992/files) (2):**
  - `tests/v1/kv_offload/cpu/test_manager.py`
  - `vllm/v1/kv_offload/cpu/policies/arc.py`

### Summary

**What changed and why**  
Optimized ARC batch eviction in vLLM's KV offload CPU manager to avoid quadratic time complexity. Previously, for each eviction candidate, the code scanned the T1 and T2 OrderedDicts from the beginning, leading to O(n^2) work when evicting many blocks. The change introduces monotonic iterators over these lists, so each entry is visited at most once during candidate collection, while preserving atomic eviction and existing eviction order.

**Technical impact**  
Reduces the time complexity of batch eviction from quadratic to linear in the number of cache entries scanned. Performance benchmarks show significant speedups (16.5x to 142x) for evicting half of a full cache. The change also adds regression tests to verify single-pass scanning, fallback behavior when T1 iterator is exhausted, and atomic failure. No user-facing behavior changes; only internal cache bookkeeping is affected.

**Potential risks**  
The new iterator-based approach relies on the assumption that the cache lists are not mutated during candidate collection. Since mutations are deferred until all candidates are found, this holds. However, if future changes introduce mutations during the loop, the iterators could become invalid. The fallback logic when T1 iterator is exhausted but virtual_t1_size remains above target is subtle and could be a source of bugs if not handled correctly. The tests cover this scenario, but edge cases with protected blocks and ref_cnt changes during iteration are not explicitly tested.

**Key insights**  
The optimization leverages the fact that eviction order is deterministic and that the lists are not modified until after candidate selection. By using iterators, the code avoids repeated scans and the need for an 'already_selected' set, simplifying the logic. The performance improvement is substantial for large caches, making this a valuable optimization for KV offload workloads. The added tests are well-targeted to ensure correctness of the new scanning behavior.

---

## 57. [[CI] Run control-plane workflows on vLLM runners](https://github.com/vllm-project/vllm/pull/51127)


### Base Information

- **PR Number:** #51127
- **Author:** [khluu](https://github.com/khluu)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-08-05 00:14:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/51127/files) (2):**
  - `.github/workflows/pre-commit.yml`
  - `.github/workflows/run-ci-command.yml`

### Summary

**What changed and why**  
Changed the runner for two CI control-plane jobs (pre-run-check in pre-commit.yml and run-ci-command in run-ci-command.yml) from GitHub-hosted ubuntu-latest to the self-hosted vllm-runners group. This was done to avoid the GitHub-hosted concurrency limit and reduce queue times.

**Technical impact**  
Both jobs now run on self-hosted runners with the label [self-hosted, linux, x64, vllm-runners]. This shifts load away from the saturated GitHub-hosted pool and may affect scheduling, resource availability, and security posture since self-hosted runners are used.

**Potential risks**  
Self-hosted runners may have different software availability or network access compared to GitHub-hosted runners, potentially causing failures. Security implications are mitigated because pre-run-check does not check out PR code and run-ci-command checks out the default branch, but the change still increases exposure to self-hosted infrastructure. Also, if the vllm-runners pool is busy, jobs could still queue.

**Key insights**  
The change is minimal (two lines) but has significant operational impact. The PR description provides thorough reasoning and validation, including actionlint checks and security analysis. The move to self-hosted runners for control-plane jobs is a pragmatic response to infrastructure limits, but monitoring is needed to ensure reliability.

---

## 58. [[XPU] Alias is_current_stream_capturing to XPU in cuda wrapper](https://github.com/vllm-project/vllm/pull/50526)


### Base Information

- **PR Number:** #50526
- **Author:** [Sundaresan-G](https://github.com/Sundaresan-G)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-08-05 00:10:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/50526/files) (1):**
  - `vllm/v1/worker/xpu_model_runner.py`

### Summary

**What changed and why**  
Added a missing alias `torch.cuda.is_current_stream_capturing = torch.xpu.is_current_stream_capturing` inside the `supports_xpu_graph()` block in `xpu_model_runner.py`. This fixes an AttributeError on XPU when code (e.g., vLLM's weight offloading prefetch) calls `torch.cuda.is_current_stream_capturing()` during CUDA graph capture.

**Technical impact**  
Enables CUDA-graph-capturing code paths to work on XPU by providing the missing API. This is a one-line change that aligns with existing aliasing for graph, CUDAGraph, and graph_pool_handle. It allows the prefetch offloader to correctly detect graph capture and use appropriate synchronization on XPU.

**Potential risks**  
Low risk. The alias is only set when `supports_xpu_graph()` is true, so it doesn't affect non-XPU or non-graph-capable environments. Potential mismatch in semantics between `torch.xpu.is_current_stream_capturing` and the CUDA version is unlikely but should be verified. No other code paths are affected.

**Key insights**  
The change is minimal and targeted, fixing a real bug in XPU compatibility. It follows the existing pattern of aliasing CUDA APIs to XPU equivalents. The PR description clearly explains the necessity and confirms no duplicate PR exists. The change is behavior-affecting and thus classified as SUBSTANTIAL.

---

